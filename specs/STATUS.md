# 規格狀態索引

> **用途**：作為所有功能規格流程狀態的單一真實來源（Single Source of Truth）。
> **更新規則**：當建立新的 spec 產物、開啟分支、開啟或封存 OpenSpec change，或功能被封存時，需更新本表。
> **封存規則**：功能實作合併至 `main` 後，執行 `mv specs/[module]/NNN-feature specs/_archive/NNN-feature`，並將 Status 更新為 `archived`。

## 狀態說明

| Status | 說明 |
|--------|------|
| `spec-ready` | 已有 `spec.md`，尚未開啟 OpenSpec change |
| `change-open` | `openspec/changes/<change>/` 已建立（`/opsx:propose` 已完成），尚未 `/opsx:archive`。進入條件：change 的 `proposal.md` frontmatter 標明對應此 spec；離開條件：`/opsx:archive` 完成（回寫正典 spec 版本號與 Changelog），轉為 `in-progress` 或 `done` |
| `in-progress` | 實作分支進行中 |
| `review` | PR 已開啟，等待合併 |
| `done` | 已合併至 `main`，尚未封存 |
| `archived` | 已移至 `specs/_archive/` |
| `deferred` | 已有 `spec.md`，但依目前 prototype / IA baseline 暫緩實作 |

---

## 功能流程（已存在 Spec 檔案）

| ID | 功能 | 模組 | 狀態 | 分支 | 備註 |
| --- | --- | --- | --- | --- | --- |
| foundation-000 | Foundation — 工程基準與共同約束 | foundation | `done` | `feat/foundation-core-*`（9 個 stacked PR，皆已合併） | spec v1.12.4；FR-001~131；SC-001~045；plan.md v2.0.0（Foundation-Core）保留為常設架構文件（ADR-033 例外，非 OpenSpec change 產物）。**Foundation-Core 範圍**已由 OpenSpec change `implement-foundation-core`（issue #356 Phase 4 pilot）實作完成並 archive；**未封存**：本 spec 為全 repo 以路徑引用的跨模組長期基線，且 F-17 Observability（FR-091~100、SC-021~028）與 Celery 相關需求依 plan.md 延後、尚未實作，封存與否待維護者裁決 |
| foundation-001 | Project SDD Lint | foundation | `in-progress` | `feat/issue-648-verification-suite-lint`（PR #658） | spec v1.2.0；正典維持於 `specs/foundation/001-project-sdd-lint/`，正式系統尚未完成前不封存。OpenSpec change `enforce-ci-job-parity`（issue #648）已 archive 為 `openspec/changes/archive/2026-09-04-enforce-ci-job-parity`，正典已回寫 FR-010、SC-009 與 AC-5.1–AC-5.6：`CI_JOB_PARITY` 以 `scripts/ci-jobs.tsv` 登錄表雙向對照 CI job 與本機驗證套件，並清償三支既有未接 CI 的套件。既有內容：OpenSpec change `implement-project-sdd-lint` 已於 PR #598 archive 為 `openspec/changes/archive/2026-09-01-implement-project-sdd-lint`；Issue #375 follow-up；blocking generated `design/system/screen-inventory.md` freshness 僅在 canonical resolved checker／target 同一 trust root 時組合既有 generator exact-sentinel contract，foreign root 固定 `INVENTORY_CHECK_CONFIG`／exit `2` 且不執行 child；control-character pathname 於 text／TSV flow 前以 `SCANNER_CONFIG`／安全 path `.`／exit `2` fail closed。command-line tooling，prototype／Frontend Ready Gate 不適用 |
| foundation-002 | User Path Map Freshness Check | foundation | `in-progress` | `feat/path-map-freshness-check` | spec v2.1.0（PR #918，issue #906）；正典維持於 `specs/foundation/002-user-path-map-freshness-check/`，正式系統尚未完成前不封存。OpenSpec change `strengthen-path-map-cited-source-freshness` 已 archive 至 `openspec/changes/archive/2026-09-24-strengthen-path-map-cited-source-freshness/`，Stage 3 追加「被指名來源」判定：指紋相符後另行重算路徑圖內文的 `<檔案>:<行號>` 引用與 `grep` 計數宣稱，失效或不符為 stale（`PATH_MAP_CITATION_UNRESOLVED`／`PATH_MAP_CLAIM_COUNT_MISMATCH`），無法解析為 exit `2`（`PATH_MAP_CLAIM_UNDECIDABLE`）。既有 spec v2.0.0（PR #831，issue #665）的 OpenSpec change 已 archive 至 `openspec/changes/archive/2026-09-18-add-user-path-map-freshness-check/`。Stage 2 改採畫面清單指紋：`design/system/user-path-map.html` 的 `<head>` 內 `path-map-screen-fingerprint` meta 記錄 `screen-inventory.md` 畫面／檢視 ID 清單的 sha256，`node scripts/check-user-path-map-freshness.mjs` 不呼叫 git、只比對指紋；已接上 `user-path-map-freshness` CI job 與 `CLAUDE.md` 本機指令，原 blocked 項目全數解除；command-line tooling，prototype／Frontend Ready Gate 不適用 |
| account-001 | Login — Email / Password | account | `spec-ready` | `feat/account/001-login-email-password` | spec v1.2.4；規格狀態：Clarified；plan.md v2.0.0 於首個 OpenSpec change 作為 design.md 參考素材，之後歸檔（ADR-033） |
| account-002 | Login — Google SSO | account | `spec-ready` | `feat/account/002-login-google-sso` | spec v1.2.3；規格狀態：Clarified |
| account-003 | Register — Email / Password | account | `spec-ready` | `feat/account/003-register-email-password` | spec v1.2.7；規格狀態：Clarified |
| account-004 | Forgot / Reset Password | account | `spec-ready` | `feat/account/004-forgot-reset-password` | spec v1.1.4；規格狀態：Clarified |
| account-005 | Profile Settings | account | `spec-ready` | `feat/account/005-profile-settings` | spec v1.2.10；規格狀態：Clarified |
| admin-006 | User Management | admin | `spec-ready` | `feat/admin/006-user-management` | spec v1.0.9；規格狀態：Clarified |
| admin-007 | Role & Permission Settings | admin | `spec-ready` | `feat/admin/007-role-settings` | spec v1.1.13；規格狀態：Draft；v1.1.13（issue #261 drift）補齊未儲存變更 banner 的介面定義（區塊 G）——v1.1.7 記錄的「移除 banner」與現行已測試、可及性語意完整的實作不符，banner 與取消確認對話框（區塊 E）為互補關係，非互斥；純文件修正，不改變任何 prototype 程式碼或測試 |
| dashboard-012 | Dashboard | dashboard | `review` | `chore/815-canon-writeback` | spec v2.12.0；規格狀態：In Progress；v2.12.0（issue #815，PR #844）FR-011D／FR-011E 示範任務範圍 T014–T017 → T014–T016、SC-024 快速操作 17 → 16；v2.11.1（issue #627 第 7 項，Lightweight Path）FR-022 同步 annotation-015 v6.3.2，刪除摘要文案中的 `未達定稿門檻 {n} 個`——FR-022 宣告 015 FR-076 為唯一來源卻逐字複述完整格式，不同步即成「015 說不顯示、012 說要顯示」的正典矛盾；無程式碼變更（兩畫面共用同一個 `formatReviewSummary()`）；**本列先前漏登 v2.11.0**（issue #533，Reviewer 總覽三張指標卡明示為示範用靜態值、新增 FR-024／SC-028），一併補記；v2.10.0（issue #501）Reviewer 任務列審查摘要全面改為推導、取消種子回退分支——FR-020 原以「是否存在可推導審核單位狀態」判定，實際使十三筆從未被審核的任務（T001–T013）永遠顯示手寫敘事字串（T003 卡面「待審 7 · 覆蓋率 34%」，其下十五筆單位列全為待審）；本版取消回退分支，`dashboard.js` 與 `annotation-list.html` 兩處消費端同一 commit 移除判定，reviewer 種子不再攜帶摘要字串；新增 FR-023 規範 IAA 改由 `computeIaaAlpha()` 推導並區分「數值／無法計算／不提及」三態；v2.9.0（issue #510）撤銷 FR-009E／SC-027——v2.5.0 為 PL 任務列表新增的「審核流程示範」快捷分類按鈕，其立論前提（T014–T017 自儀表板「查看全部」不可發現）經複查僅前半成立：`查看全部` 導向的任務清單未帶 `keyword`，17 筆種子全量同頁列出，四筆示範任務本就在其中，該按鈕僅省去一次關鍵字輸入；維護者裁定移除，同步移除原型 `renderDemoShortcuts()`、`demoCategory`／`demoCategories` 與對應測試檔，T014–T017 任務本身與其餘欄位全數保留；`SC-027` 為已撤銷編號不重用、後續 SC 編號不重編；v2.8.1（issue #472）FR 編號整理——`FR-0170`／`FR-0170A` 尾碼使用阿拉伯數字 `0`，脫離 `FR-017A`–`FR-017K` 的字母子序列且位置夾在 `FR-017` 與 `FR-017A` 之間；改名為 `FR-017L`／`FR-017M` 並搬移至 `FR-017K` 之後，使全序列連續遞增，需求文字逐字未改，改號前確認無下游引用；v2.8.0（issue #452）Reviewer 任務列審查摘要改為寫出計數主體與分母——原「審核覆蓋率 100%」與 17 筆種子摘要「進度 {p}%」皆只給百分比不給分母，新增 FR-022：摘要文案改為「任務覆蓋 x/n 個審核單位・待審 n 個・未達定稿門檻 n 個・爭議中 n 個」，文案定義以 annotation-015 FR-076 為唯一來源、dashboard 不維護第二套措辭，`coveragePct` 公式與排序欄位不變（本版號與對應 015 v4.31.0／FR-076／AC-1.24 須同 PR 合併，且須先於 015 v4.32.0／FR-077／AC-3.42 合併）；v2.7.0（issue #449）Reviewer「快速審核」改為導向下一個可處理審核單位——原本 T014–T017 目標樣本固定為資料集第一筆，導致多筆命中已定稿唯讀卡且與 CTA 名稱不符，新增 FR-021：目標單位改由 annotation-015 FR-073 `findNextActionableReviewUnit()` 依優先序（待審 → 可仲裁爭議 → 未達門檻且本人未提交）推導，導頁攜帶完整審核單位身分參數，無可處理項目時導向清單空狀態；同步修訂 FR-011D 示範任務固定第一筆之例外（本版號與對應 015 v4.28.0／FR-073 須同 PR 合併）；v2.6.0（issue #450）Reviewer 任務列審查摘要改由審核單位狀態推導——原 `detail` 為預組靜態字串，審核員完成審核或仲裁後任務卡數字不會更新，新增 FR-020：四項計數與審核覆蓋率一律取自 annotation-015 FR-072 `computeReviewSummary()`（唯一公式來源），進度條寬度改用推導覆蓋率，適用判定改以「是否存在可推導審核單位狀態」而非任務 ID 白名單；同步修訂 FR-011E 適用範圍；v2.5.0（issue #404）PL Dashboard 任務列表新增資料驅動的示範任務快捷分類入口（新增 FR-009E、SC-027，詳見變更紀錄）；v2.4.1（issue #261 drift，FR-018／FR-019）新增 `task_membership` 模擬非同步載入的 Skeleton 與 API 錯誤／重試狀態（規格條文本已正確，僅原型未落實）；v2.4.0（issue #187）Annotator／Reviewer 任務列表新增依進度排序下拉控制（新增 FR-010D、FR-011F、SC-026）；排序不影響既有卡片欄位、CTA 與導頁行為；因資料模型未提供「最後提交時間」／「待審筆數」獨立欄位，未新增此二排序鍵，「使用者實際被指派子集」亦受限於現有 per-task 單一 work item 模型而維持現況全量清單，未新增個人化篩選；v2.3.1（issue #311，PR #315）prototype 合規修正（無新增 FR）——Annotator／Reviewer 任務卡互動列補 `role="button"`／`tabindex="0"`／`aria-label` 與 Enter/Space 鍵盤觸發（沿用 issue #186 FR-008F 指標卡模式，內層快速操作按鈕不受列攔截），列點擊導頁 URL 恢復 FR-010B1／FR-011B1 要求的 `task_type` 參數（恢復 `annotationTaskType` 逐任務 compatibility seed，值不得由 `outputs[]` 推導；2026-08-10 eff1938 遷移移除欄位但未同步 spec 的歷史回歸，legacy `activeTaskType` localStorage 寫入不在範圍）；v2.3.0（issue #310，PR #312）FR-011E 示範列審查摘要覆蓋率語意——T014–T017 reviewer 示範列百分比統一標示為「審核覆蓋率」（本人待審清空比例，非定稿進度），T016 摘要改「審核覆蓋率 100% · 未定稿 3 筆 · 爭議 1 筆」揭示仲裁待辦，捨棄誤讀為任務完結的「待審 0 ·進度 100%」（雙軌驗收 Codex [High]；數字維持靜態 seed 且與 015 seeder 矩陣一致）；v2.2.0（issue #302，PR #305）FR-011D 審核流程示範任務 reviewer 身分導覽——示範列攜帶 `reviewerId = reviewer_chen` 身分 seed，列點擊與 `快速審核` 網址附帶 `reviewer_id`，`快速審核` 的 `sample_id` 固定指向資料集第一筆（仲裁初始畫面自儀表板可達；僅 T014–T017 示範列）；v2.1.0（issue #302）Annotator／Reviewer 場景基線由 T001–T013 擴充為 T001–T017（四筆 `single_label` 審核流程示範任務，reviewer 工作列數字依 boot seeder 審核狀態矩陣呈現；SC-025 對帳基線註明 T014 不計入待 IAA 筆數）；v2.0.5（issue #186）「等待 IAA 確認」指標卡新增可執行待辦入口（role=button、鍵盤可操作，導向 `/task-list?status=waiting_iaa_confirmation`），新增 FR-008F、FR-009A1、SC-025；v2.0.3（issue #191）Reviewer 行動卡文案對齊 annotation-015 審核模型（逐標記員審核、步驟 2 改「修正或仲裁」、退回僅限 official_run）；各角色任務摘要依 `outputs[].type` 順序顯示多 tag；Annotator／Reviewer prototype 皆完整呈現 T001–T013，並以獨立 task／sample／compatibility route 導向對應角色介面；13 筆僅為安全示例，第 14 筆合法組合仍可 config-driven 呈現；正式 membership 與 014／015 consumer 邊界不變 |
| shared-008 | Shared Sidebar Navbar | shared | `spec-ready` | `feat/shared/008-sidebar-navbar-shared` | spec v1.6.0；規格狀態：Clarified；OpenSpec change `fix-944-sidebar-role-aware-labels`（issue #944，維護者裁定採方向 A——收斂到來源，PR 待開）已 archive 為 `openspec/changes/archive/2026-09-25-fix-944-sidebar-role-aware-labels/`：新增使用者故事 9、FR-020／FR-020A、SC-013／SC-013A——`navAnnotation`（L0「標記作業」）與 `roleIndicator`（使用者角色標示）依任務角色（`project_leader / reviewer / annotator`）解析改由 `sidebar.js` 之 `renderSidebar()` 內部完成（新增 `opts.taskRole`，內建雙語對照表比照既有 `adminSubmenuI18n` 前例），移除 `annotation-workspace.config.js` 兩處消費端 DOM 覆寫（issue #309、#931），與 annotation-015 FR-080 入口麵包屑既有角色分流一致（FR-080 本身未變更）；既有 `opts.roleIndicator` 顯式覆寫入口優先序不變；狀態由 `change-open` 回到 `spec-ready`；分支欄與正典 frontmatter `功能分支` 同為 `feat/shared/008-sidebar-navbar-shared`，不隨本次變更分支調整；issue #932（Lightweight Path）澄清 FR-001：`userAvatar` 須顯示與同列 `userName` 一致之縮寫，所有 `SUPPORTED_PAGES` 皆同，空白／零寬字元則回退既有預設佔位文字「U」；OpenSpec change `admin-role-settings-nav-shortcut`（issue #725，PR #757）已合併並 archive：新增使用者故事 8、FR-019 群、SC-012 群，`super_admin` 側欄「系統管理」提供可展開次選單直達 `role-settings`，不改變既有 L0 導覽項清單與計數（FR-002／FR-003A／SC-003）；v1.4.4（propose 前置）補齊缺漏的 `## 功能目標` 標題（Project SDD lint ratchet，規格條文未變）；v1.4.3（issue #409）修正快捷鍵總覽 `R` 列標籤誤導性文案——v1.4.1 加上的「（限正式標記）」註記字面上讀起來像整個退回動作與 `R` 快捷鍵皆僅限 `official_run`，但退回控件與 `R` 鍵在 `dry_run` 同樣可用（annotation-015 AC-3.33 禁止審核卡依 `run_type` 分流呈現），僅「退回後把標記員狀態回退為待標記」此一副作用才是 `official_run` 專屬；標籤字面由「退回目前結果（限正式標記）」改為「退回目前結果（回退標記員狀態僅限正式標記）」，將限定範圍精確掛在該副作用上（規格條文未變，FR-016G／SC-009D 既有行為之標籤字面精確化）；v1.4.2（issue #261）新增 Prototype Traceability，明確對應共用 `sidebar.js`／`sidebar.css` 與 living styleguide 參考頁的責任邊界；v1.4.1（issue #191）快捷鍵總覽 `R` 列標籤改「退回目前結果（限正式標記）」（en：Return current result (formal runs only)），標注 official_run 專屬範圍；v1.4.0（issue #146 P2）快捷鍵總覽 `審核` section 移除批次兩列（`Shift+A` / `Shift+R`），只保留 `A` / `R`（FR-016G、SC-009D 修訂） |
| shared-018 | Help Button — 平台說明入口 | shared | `deferred` | `feat/shared/018-help-button` | spec v1.1.1；最新 sidebar prototype baseline 尚未提供 Help button / Help modal |
| annotation-015 | Annotation List + Workspace | annotation | `in-progress` | `fix/914-finalized-value-validation` | spec v6.21.0；規格狀態：In Progress；v6.21.0（issue #914，Lightweight Path；未新增或移除任何 FR／AC，僅釐清既有條文）：狀態推導未驗證定稿值合法性，取值端與狀態推導各寫一套判準——`getReviewUnitStatus()` 判定「全數已解決」時，對仲裁與例外池紀錄僅檢查 `finalized_by`／`action` 是否存在，未如取值端 `getFinalizedOverwrites()` 要求該紀錄真正持有合法 `finalized_value`，使缺少或不合法 `finalized_value` 之紀錄仍誤判為 `已定稿`，答案欄靜默回退為標記員原答案卻仍顯示定稿依據徽章。維護者複驗（2026-09-24）擴大範圍兩處：仲裁分支與例外池分支皆有此寬判準；`custom_answer` 未選答案時可定稿出 `finalized_value: null`，同樣違反 AC-4.56 隱含之合法值要求。`annotation-workspace.data.js` 新增單一共用判準 `hasLegitimateFinalizedValue()`（`action === 'custom_answer'` 時額外拒斥 `null`，其餘情形僅要求屬性存在），套用於 `getReviewUnitStatus()` 兩分支與 `listReviewPoolItems()`；`annotation-list.html` 之 `getFinalizedOverwrites()`／`getFinalizationSourceKeys()` 改呼叫同一判準。獨立審查攔下一項實作陷阱：`finalized_value: null` 於仲裁 `adopt_b`／例外池 `adopt_reviewer` 承接審核員 `bypass` 決策時是既有合法之「無法判定」語意（design.md D3），非一律不合法，判準僅在 `custom_answer` 時額外拒斥。範圍外發現（另案追蹤，本版不動）：`annotation-workspace.config.js` 另有 4 處同構寬判準（`finalizedAnswers()`、`finalizedBasisLabels()`、仲裁列表開放項過濾、`exceptionPoolQueue()`）；`arbitrationFinalizedSnapshot()` 之 `[0]`（issue #975）維持不動。正典依 issue #578／#596／#907／#910／#913／#921 先例留在 `specs/annotation/015-annotation-workspace/`，狀態維持 `in-progress`；v6.20.0（issue #913，Lightweight Path；未新增或移除任何 FR／AC，僅釐清既有條文）：例外池「採審核員答案」與仲裁「採 B」取錯提交者——`buildExceptionPoolItemRow()`／`renderArbitrationCard()` 皆以 `readReviewerSubmissions(...)[0]`（bucket key 字典序）取「該單位審核員」，非 FR-093（1）已定義之 sticky 擁有者；遺留資料出現同一單位多位提交且字典序最前者與標記員一致時，取得 `undefined` 並寫入 `finalized_value`，遭 `JSON.stringify` 靜默丟棄該鍵，紀錄缺 `finalized_value` 卻仍判 `已定稿`。維護者複驗（2026-09-24）更正產生途徑：非名冊異動，而是工作區此前缺送出指派閘門（已由 issue #921／v6.17.0 補上），該閘門關閉後產生途徑僅剩舊提交殘留，但取值邏輯仍須修正，範圍複驗確認需同時涵蓋仲裁 `adopt_b`。`annotation-workspace.data.js` 新增 `getStickyReviewerId()`（重用既有 `getStickyReviewers()`／`stickyUnitKey()`，不另立第二套）；`annotation-workspace.config.js` 新增共用 `resolveStickyReviewerSubmission()` 取代兩處 `[0]`。釐清 FR-061 第 2 點與 FR-095（`adopt_reviewer`）：明文遺留多提交形狀下之取值須沿用 FR-093（1）sticky 擁有者，不新增條文編號。範圍外發現（另案追蹤，本版不動）：`arbitrationFinalizedSnapshot()`（`adopt_b` 分支）另有第三處 `[0]`，僅影響 `adjudicated` 歷程事件之 `result_snapshot`，非 `finalized_value`。正典依 issue #578／#596／#907／#910／#921 先例留在 `specs/annotation/015-annotation-workspace/`，狀態維持 `in-progress`；v6.19.0（issue #908，OpenSpec change `fix-908-annotator-finalized-lock` 已 archive 為 `openspec/changes/archive/2026-09-25-fix-908-annotator-finalized-lock/`，MINOR）：標記員定稿鎖定——`official_run` 審核單位一旦依 FR-051 推導為 `已定稿`，標記員自身之後續寫入此前完全無守衛，`markSampleSubmitted()`／`markSampleSaved()`／`appendSampleTimelineEvent()` 三個標記員寫入點皆可直接改動已定稿之作答；維護者裁示鎖定觸發條件不得只看單位狀態為 `finalized`，須是「該受審標記員存在真實已提交紀錄」——FR-044a 示範列 seed 頂替出的審核單位，其標記員本人可能從未真正提交過，對其顯示「已定稿」屬假訊息；新增 FR-101（對應新增 AC-2.27）：寫入側守衛沿用既有 `getReviewUnitStatus()` 判定式（其首句即要求真實提交存在才可能推導為非 `null`），不另立第二套判定，使示範列 seed 單位天然豁免；鎖定時三個寫入函式回傳 `false` 且不產生任何寫入，未鎖定時回傳 `true`；outKeys 由 `resolveTaskProfile(taskId).outputs` 內部推導，三個函式簽章不變；鎖定範圍僅 `official_run`，`dry_run` 不受影響；畫面渲染 `ws-annotator-finalized-notice` 資訊色鎖定提示，`wsSkipBtn`／`wsSaveBtn`／`wsSubmitBtn` 與作答控件保留在畫面但套用原生 `disabled`＋`aria-disabled`；自動儲存狀態列改顯示「已定稿，不再自動儲存」；不引入任何持久化定稿快照（沿用 FR-072(3) 既有讀取時計算、不快取原則），不新增任何一般解鎖入口；同步修訂 FR-072 第 3 點，把「標記員重新提交」限定為「未定稿審核單位之標記員重新提交」；正典依 issue #578／#596／#907／#910／#921 先例留在 `specs/annotation/015-annotation-workspace/`，故狀態維持 `in-progress` 而非 `archived`；v6.18.0（issue #910，OpenSpec change `fix-910-review-unit-status-consistency` 已 archive 為 `openspec/changes/archive/2026-09-25-fix-910-review-unit-status-consistency/`，MINOR）：審核單位狀態推導單一來源化與審核決策重複送出去重——不推翻 FR-044a／FR-053（示範列可作為審核 seed、空單位閘門雙條件判定皆維持原狀），修訂 FR-064 第 2 點補齊與 FR-053 同源之示範列限定語（工作區頂部橫幅與左欄清單審核單位狀態標籤共用同一推導，皆顯示待審，不再出現橫幅「尚無標記提交」與左欄「待審」並存的不一致），新增 FR-016B 段落將既有連續重複 `submitted` 事件去重（issue #201）擴及審核決策事件（以 outKey 為比對單位）；實作與獨立審查過程中另發現並修正一個由後者引入之 FR-088／AC-2.26 計時迴歸（`timingWritten` 旗標於確認事件寫入前就被消耗）；正典依 issue #578／#596 先例留在 `specs/annotation/015-annotation-workspace/`，故狀態自 `change-open` 回到 `in-progress` 而非 `archived`；v6.17.0（issue #921，OpenSpec change `gate-review-assignment` 已 archive 為 `openspec/changes/archive/2026-09-25-gate-review-assignment/`，MINOR）：工作區補上審核指派之送出閘門——`annotation-list.html` 已於 issue #824 依 `getAssignedReviewUnits()` 過濾清單列，但工作區互動閘門 `reviewUnitBlockReason()` 從未檢查指派，任一在職審核員皆可在工作區以直接網址開啟他人審核單位並送出決策，造出 FR-093 明文禁止的多提交形狀（issue #913／#914 之實際產生途徑）；修訂 FR-093（新增送出閘門段落），新增 AC-4.70、AC-4.71；`annotation-workspace.config.js` 新增 `isCurrentUnitAssigned()` 與 `REVIEW_UNIT_BLOCK.NOT_ASSIGNED`，`reviewUnitBlockReason()` 判定序改為 ARBITRATION → FINALIZED → OFF_ROSTER → EMPTY → NOT_ASSIGNED；維護者裁定另包含之左欄／導覽指派過濾牽動約 48 個既有測試檔、遠超單一 PR 範圍，經升級判定移交 issue #956（前置本 issue）；正典依 issue #578／#596／#907 先例留在 `specs/annotation/015-annotation-workspace/`，故狀態自 `change-open` 回到 `in-progress` 而非 `archived`；v6.16.2（issue #931，Lightweight Path）審核員視角側欄「標記作業」項目字面與麵包屑不一致——共用側欄 L0 項目字面固定為預設值，FR-080 麵包屑首層已依角色分流顯示「審核作業」／「標記作業」，但側欄項目從未分流；`annotation-workspace.config.js` 之 `applyStaticI18nText()` 於 `currentRole === 'reviewer'` 時重用既有 `crumbWorkAreaReviewer` 設定值覆寫側欄 `#navAnnotation` 文字，不另立新字串，`sidebar.js` 契約不變；未新增或移除任何 FR／AC；v6.16.1（issue #934，Lightweight Path）補上工作區缺漏的 `<main>` landmark 與 `<h1>`：`#wsRoot` 由 `<div>` 改為 `<main>`，新增 sr-only `<h1>` 鏡射既有 `#entryBreadcrumb`（FR-080）已呈現之角色感知任務／樣本脈絡並隨導覽同步；未新增或移除任何 FR／AC；v6.16.0（issue #907，OpenSpec change `pl-exception-disposal-screen-shell` 已 archive 為 `openspec/changes/archive/2026-09-24-pl-exception-disposal-screen-shell/`，MINOR）：專案負責人之最終例外處置畫面改用例外池專屬外殼——左側清單只列該任務該 `run_type` 的待處置例外項、計數與上／下一筆導覽以例外項為單位、不顯示標記提交進度文案與自動儲存狀態列、每項就地呈現仲裁者與「兩者皆非」理由、僅 `exclude_from_dataset` 帶危險樣式；修訂 FR-095（新增畫面外殼段落）、新增 AC-4.69 與 SC-011，四個處置動作集合、`run_type` 分流與各自資料寫入契約（AC-4.56、AC-4.57）維持原狀；正典依 issue #578／#596 先例留在 `specs/annotation/015-annotation-workspace/`，故狀態自 `change-open` 回到 `in-progress` 而非 `archived`；v6.15.1（`f7a31025`，Lightweight Path）釐清歷程摘要 fallback，當時漏更新本列（本列先前仍停留在 v6.15.0 與 `fix/868-arbitration-reserve-apply`（PR #877，merge `114f28cd`））；正式系統尚未完成，正典維持於 `specs/annotation/015-annotation-workspace/`。issue #868 companion change `reserve-arbiters-from-review-assignment` 已 archive：指定於 `arbiter_ids` 的成員保留處理仲裁，不納入新審核單位的自動分派；既有黏住提交維持歷史歸屬；仲裁資格改讀 task profile；v4 demo migration 僅在必要 seed 逐筆驗證成功後提交完成 marker，失敗保留舊 marker 供重試。 |
| task-management-010 | Task List | task-management | `in-progress` | `chore/815-canon-writeback` | spec v2.2.0；規格狀態：In Progress；v2.2.0（issue #815，PR #844）示例基線 17 → 16 筆，移除平手示範任務 T017；v2.1.1（issue #261）新增 Prototype Traceability，明確對應 task-list 原型頁面、資料層與設計層參考的責任邊界；v2.1.0（issue #302，PR #305）示例基線擴充至 17 筆——新增 `review-flow-*.json` 四筆 `single_label` 示範任務（T014–T017），`single_label` 命中數基線 2 → 6；T014 以 `dry_run_in_progress`、T015–T017 以 `official_run_in_progress` 起始，原 13 筆 draft 起始約定不變；列表改由 `outputs[].type` 顯示多 tag，提供 registry-driven 8 輸出類型 membership 篩選與 `limit`／`offset` 分頁；13 筆示例 seed 統一以 `status = draft` 起始（014 draft 編輯情境），非 draft 行為由測試合成任務驗收；012 Dashboard 與 016 list 已同步，015／017 consumer 仍延後 |
| task-management-013 | New Task (+ Config Builder) | task-management | `in-progress` | `fix/811-bypass-task-surfaces` | spec v8.1.1；規格狀態：Draft；v8.1.1（issue #811，PATCH，隨 annotation-015 v6.8.0 同步回寫，PR #860（`278e4872`））FR-003j `allow_bypass` 開關英文文案改為 `Allow "Unable to determine (Bypass)"`，中英文皆改由 `BYPASS_WORDING` 組成，行為不變、未新增或移除任何 FR／AC／SC 編號；v8.1.0（issue #755，OpenSpec change `task-new-step1-field-role-hints` 已 archive 為 `2026-09-19-task-new-step1-field-role-hints`，PR #853（`d3f806a8`），MINOR）：Step 1 欄位角色初始值依欄名自動推測——尚未指定過角色之欄位若欄名命中 `FIELD_ROLE_INPUT_NAME_HINTS`，依原始出現順序指定為 Input（至多達輸入類型所需數量），其餘維持 FR-002c-1 預設「不使用」；Evidence／Output 永久不自動推測（Data Fairness）；新增 FR-002c-8、AC-1.6、AC-1.7、SC-002h；v8.0.0（破壞性，issue #724，OpenSpec change `remove-task-new-step1-type-preset` 已 archive 為 `2026-09-16-remove-task-new-step1-type-preset`）：移除 Step 1 常用組合一鍵預設——FR-002f、AC-1.4、AC-1.5、SC-002g 與對應邊界情況全數刪除；本版預設清單僅 1 筆卻佔據任務類型欄位最上方一整列，並多加一層介面概念，投報率不成立。任務類型選擇回到 FR-002／FR-002a–FR-002e 之三段式模型，選擇語意、cascade、`deriveTaskType()`、`validateStep1()` 與提交 payload 形狀皆不變；issue #724 之方向 ②／③ 仍由 issue #755 追蹤；v7.1.0（issue #724，OpenSpec change `task-new-step1-type-preset` 已 archive 為 `2026-09-13-task-new-step1-type-preset`）：Step 1 任務類型選擇新增常用組合一鍵預設（本版 1 個：`classification` + `single_item` + `single_label`），三段式選擇器維持完整可見與可個別再調整；新增 FR-002f、AC-1.4、AC-1.5、SC-002g，不修改既有條文；issue #724 之方向 ②／③ 留待 issue #755 追蹤；分支欄同步校正正典 frontmatter（原 `fix/issue-659-sequence-tagging-overlap-feedback` 為該分支已合併後未同步之舊值）；v7.0.1（issue #659，patch，Lightweight Path）釐清 FR-003d-1 相交拒絕回饋涵蓋「新圈選範圍完全落在既有 span 內」情形——v7.0.0 條文原僅描述色條轉為錯誤色，未涵蓋無暴露字元可上色的完全覆蓋情境，導致該情境下畫面無任何回饋；補上被撞到既有 span 的危險色虛線外框、拒絕訊息列（`role="alert"`）與「點擊標籤類型不清除拒絕狀態暫存選取」三項回饋規則，無新增或移除 FR/AC；v7.0.0 為 OpenSpec change `seq-tagging-span-config`（issue #581）已 archive 為 `2026-09-04-seq-tagging-span-config` 並回寫正典：`sequence_tagging` 由 Token＋BIO 契約破壞性改為半開區間 `spans[]`（`{ start, end, label }`）與 `snap_unit` 選取吸附，`entity_recognition` 共用同一套圈選元件，新增型別層不變式 `SPAN_OVERLAP_POLICY_BY_OUTPUT_TYPE`；v6.9.6 為四組使用者故事的 40 條驗收情境配發 AC-N.N 穩定 ID（無條文變更）；v6.9.5（issue #492 A4/A5）`TaskGuidelineConfig` 新增 `guideline_version` 欄位；v6.9.4（issue #261）新增 Prototype Traceability，明確對應 task-new 原型頁面與共用 `OUTPUT_TYPE_REGISTRY` 設定引擎的責任邊界；`item_pair` 新增「項目對名稱」設定卡（預設 Input 欄位名稱、可編輯、同步預覽小標與 `item_pair_labels` 序列化）；`free_text` 與 `sequence_tagging` 預覽移除輸出卡片標題（`hidePreviewTitle`），`free_text` 另移除兩條內部分隔線，`free_text` 說明欄位與 `multi_label`「所有層級皆可選」提示 helper text 改為 tooltip（`hintAsTooltip`）；Step 1 選擇狀態明確為 `selected_categories[]` + `input_type` + `selectedOutputTypes[]`，一對一產生 `outputs[].type`；010／012／016 consumer 已同步輸出類型展示；正式 Annotation Workspace 及 014 consumer 仍延後；v6.9.1：`SAMPLING_DEFAULTS_BY_CATEGORY` generation 列建議 IAA 由 0.70 改為不適用（free_text 不計自動 IAA）；v6.9.2（issue #285）：`submitTask()` 修正為將精靈結果寫入 `labelsuite.createdTasks` prototype 資料層，建立的任務以 `draft` 狀態出現在 010 任務清單並可從 014 task-detail 開啟，規格條文未變；v6.9.3（issue #197）補齊精靈離頁保護（FR-007a／v3.3.1 sessionStorage 草稿）回歸測試，條文未變 |
| task-management-014 | Task Detail (incl. task-member-management/work-log) | task-management | `in-progress` | `fix/868-arbitration-reserve-apply`（PR #877，merge `114f28cd`） | spec v4.2.0；正式系統尚未完成，正典維持於 `specs/task-management/014-task-detail/`。issue #868 companion change `validate-reviewer-arbiter-role-separation` 已 archive：審核設定儲存時須保留至少一位未被指定為仲裁者的審核員，發布時亦依當下成員狀態重算有效分派池；可修正錯誤以 assertive live alert 呈現；`arbiter_ids` 仍允許留空。 |
| dataset-016 | Dataset Analysis List | dataset | `in-progress` | `feat/dataset-analysis-output-types` | spec v2.1.2；規格狀態：In Progress；v2.1.2（issue #261 drift）更正「Prototype 示例資料基線」IAA 示意分布敘述（`pass=6`／`not_started=2` 為 `not_applicable` 徽章狀態引入前的舊值，實際與既有測試皆為 `pass=5`／`not_started=1`／`not_applicable=2`），純文字修正；v2.1.1（issue #261）新增 Prototype Traceability，明確對應 dataset-analysis-list 原型頁面與設計層參考的責任邊界；列表以 `outputs[].type` 顯示多 tag，提供 8 類 membership 篩選、13 筆非上限示例與 `limit`／`offset`；v2.1.0：`IAA_BADGE_STATES` 新增 `not_applicable`（FR-004A、`IAAStatusSummary`），dataset-017 v2.0.0 已完成 8-key 遷移（舊「017 pending sync」範圍註記移除） |
| dataset-017 | Dataset Quality Tab (IAA / Anomaly Detection) | dataset | `in-progress` | `docs/783-iaa-precondition-017-values-on-main` | spec v3.0.1；規格狀態：Draft；v3.0.1（PATCH，issue #783 第 3 點，OpenSpec change `dataset-quality-entity-value-alignment` 已 archive 為 `openspec/changes/archive/2026-09-18-dataset-quality-entity-value-alignment/`，PR #840（`d9520f43`）：關鍵實體 `SharedMetrics` 改為 `SHARED_METRICS` 五欄位並採用 `completion_rate`，`AnnotatorRiskAssessment.risk_level` 維持三值、資料不足時為 null；FR-039 第 1 點補一句「IAA 計算尚未結束不是 IAA 結果」的交叉引用；正典依 issue #578／#596 先例**留在** `specs/dataset/`，故狀態自 `change-open` 回到 `in-progress` 而非 `archived`）；OpenSpec change `seq-tagging-span-export-metrics`（issue #581 change ③，PR #746）已 archive 為 `2026-09-09-seq-tagging-span-export-metrics` 並回寫正典 v3.0.0（BREAKING）：`sequence_tagging` 匯出層新增 BIO／BIOES／IOB2 決定性推導契約（FR-041）與詞級 tokenizer metadata／對齊擴張報告（FR-042），IAA 指標全面改 span-level（FR-012L 改 Krippendorff u-α、FR-009L 統計母體改 span、FR-035／FR-036 改 pairwise span F1 與合併聚合參考值 F1），`IAA_THRESHOLD_TOKEN` 與 Token-level Alpha 一併廢止且 ID 不重用，新增 `IAA_UNCALIBRATED_TYPES`（FR-043）——僅顯示數值與排序、不做門檻判定，待第一批 `dry_run` 實證校準；同版另新增使用者故事 5（AC-5.1～AC-5.4）與 AC-3.18、新增 SC-033～SC-036、淘汰 SC-024（其所述 `O` 遮罩規則已不存在，編號不重用）；匯出對話框 UI、匯出入口與匯出記錄表屬 `task-management/014-task-detail`，依「一 change 一正典」規則由 issue #742 承接的 companion change 落地；正典 spec 依 issue #578／#596 先例**留在** `specs/dataset/`，故狀態自 `change-open` 回到 `in-progress` 而非 `archived`；v2.1.2（issue #261 drift，FR-002）修正 `dataset-analysis-detail.html` 未知／缺漏 `task_id` 靜默回退 T001 的原型缺陷——改為導回 `dataset-analysis-list.html` 並顯示錯誤 toast（規格條文本已正確，僅原型未落實）；v2.1.1（issue #261）新增 Prototype Traceability，明確對應 dataset-analysis-detail 主頁 shell、Stats／Quality 16 個 partial 與設計層參考的責任邊界；v2.1.0（issue #153）：新增 FR-038 定義標記員風險表建議行動按鈕導頁——「審核標記」→ `/annotation-list?task_id=…&role=reviewer&run_type=dry_run`、「調整參與狀態」→ `/task-detail/:task_id?tab=member-management`（僅 project_leader，沿用 FR-029；標記員篩選預填因兩側 roster 未對齊留待後續）；IAA 框架全面遷移至 ADR-029 `outputs[]` 8-key 模型——新增 `OUTPUT_TYPE_IAA_REGISTRY` 逐型指標表（含門檻常數）、x/y 達標徽章合成規則（`IAA_GATE_EXCLUDED_TYPES=free_text` 排除分母、閘門=全部達標）、`IAA_SUMMARY_STATES` 新增 `not_applicable`、小樣本警示（n<5）、sequence_tagging token-α O-tag 遮罩規則；退役全部 legacy taxonomy 常數/FR/SC/實體（詳見 spec Changelog v2.0.0） |

---

## 待辦清單（Spec 檔案尚未建立／需重建）

| ID | 功能 | 目標模組 | 規劃備註 |
|---|------|----------|----------|
| — | — | — | — |

---

## 變更紀錄

| 日期 | 更新內容 |
|------|----------|
| 2026-09-25 | `annotation-015` 更新至 v6.20.0（issue #913，Lightweight Path；未新增或移除任何 FR／AC）：例外池「採審核員答案」與仲裁「採 B」皆以 `readReviewerSubmissions(...)[0]`（bucket key 字典序）取「該單位審核員」，非 FR-093（1）已定義之 sticky 擁有者——遺留資料出現同一單位多位提交且字典序最前者恰與標記員一致時，取得 `undefined` 並經 `JSON.stringify` 靜默丟棄 `finalized_value` 鍵，紀錄缺值卻仍判 `已定稿`。維護者複驗（2026-09-24）更正產生途徑為「工作區此前缺送出指派閘門」（已由 issue #921／v6.17.0 補上），範圍確認需同時涵蓋仲裁 `adopt_b`。新增 `getStickyReviewerId()`（重用既有 `getStickyReviewers()`）與共用 `resolveStickyReviewerSubmission()` 取代兩處 `[0]`；釐清 FR-061 第 2 點與 FR-095，不新增條文編號。狀態維持 `in-progress`。 |
| 2026-09-25 | `shared-008` 更新至 v1.6.0，OpenSpec change `fix-944-sidebar-role-aware-labels` 已 archive 為 `openspec/changes/archive/2026-09-25-fix-944-sidebar-role-aware-labels/`（issue #944，MINOR）：新增使用者故事 9、FR-020／FR-020A、SC-013／SC-013A——`navAnnotation`／`roleIndicator` 依任務角色解析收斂至 `sidebar.js` 來源，移除 `annotation-workspace.config.js` 兩處消費端覆寫（issue #309、#931），與 annotation-015 FR-080 一致；狀態由 `change-open` 回到 `spec-ready`。PR 尚未開啟，待開後於同一 PR 完成合併。 |
| 2026-09-25 | `shared-008` 完成 `/opsx:propose`，OpenSpec change `fix-944-sidebar-role-aware-labels` 已建立（issue #944：共用側欄 `navAnnotation`（標記作業／審核作業）與 `roleIndicator`（一般使用者／審核員）皆只由寫死字串渲染，`annotation-workspace` 頁面因此在消費端 `applyStaticI18nText()` 以直接 DOM 操作補寫，issue #309 與 #931 各自重複此問題。維護者裁定採方向 A——收斂到來源：`sidebar.js` 新增 `opts.taskRole` 解析，`navAnnotation`／`roleIndicator` 之角色相依文字改由來源內部完成，移除兩處消費端覆寫），狀態由 `spec-ready` 更新為 `change-open`。 |
| 2026-09-25 | `annotation-015` 更新至 v6.19.0（issue #908，OpenSpec change `fix-908-annotator-finalized-lock` 已 archive 為 `openspec/changes/archive/2026-09-25-fix-908-annotator-finalized-lock/`，MINOR）：標記員定稿鎖定——`official_run` 審核單位一旦依 FR-051 推導為 `已定稿`，標記員自身之後續寫入此前完全無守衛；維護者裁示鎖定觸發條件不得只看單位狀態為 `finalized`，須是「該受審標記員存在真實已提交紀錄」，FR-044a 示範列 seed 頂替出的審核單位不得誤鎖。新增 FR-101（對應新增 AC-2.27）：寫入側守衛沿用既有 `getReviewUnitStatus()` 判定式，不另立第二套判定，使示範列 seed 單位天然豁免；`markSampleSubmitted()`／`markSampleSaved()`／`appendSampleTimelineEvent()` 三個標記員寫入點鎖定時回傳 `false` 且不產生任何寫入；鎖定範圍僅 `official_run`；畫面渲染資訊色鎖定提示 `ws-annotator-finalized-notice`，三個既有控件保留在畫面但套用原生 `disabled`＋`aria-disabled`；不引入任何持久化定稿快照，不新增任何一般解鎖入口。同步修訂 FR-072 第 3 點，把「標記員重新提交」限定為「未定稿審核單位之標記員重新提交」。正典依 issue #578／#596／#907／#910／#921 先例留在 `specs/annotation/015-annotation-workspace/`，狀態維持 `in-progress`。 |
| 2026-09-24 | `annotation-015` 更新至 v6.16.1（issue #934，Lightweight Path）：補上 `annotation-workspace.html` 缺漏的 `<main>` landmark 與 `<h1>`——`#wsRoot` 由 `<div>` 改為 `<main>`，新增視覺隱藏（sr-only）之單一 `<h1>`，鏡射既有 `#entryBreadcrumb`（FR-080）已呈現、已依角色正確區分的任務／樣本脈絡並隨樣本導覽同步，不新增第二套文案來源、不修改 `annotation-workspace.config.js`。屬既有隱含 landmark 語意（v4.18.0 Changelog 口語提及）之落實與可及性缺陷修正，未新增或移除任何 FR／AC。狀態維持 `in-progress`。 |
| 2026-09-24 | `shared-008` 更新至 v1.5.1（issue #932，Lightweight Path）：澄清 FR-001——規格原文未明確規範 `userAvatar` 的顯示內容，導致實作僅 dashboard 頁呼叫 `updateUserChip({ avatarLabel })` 帶入正確縮寫，其餘頁面停留在寫死的預設佔位文字「U」，與同列 `userName` 顯示的實際使用者姓名不一致。修法（`design/prototype/pages/shared/sidebar.js`）：新增 `computeAvatarInitials(name)`，接入 `renderSidebar` 初次渲染與 `updateUserChip` 的 `userName` 同步路徑（僅在未帶 `avatarLabel` 時），使所有 `SUPPORTED_PAGES` 首次渲染即依 `userName` 顯示正確縮寫；`userName` 為空白、僅含空白字元或僅含零寬字元（`U+200B`／`U+200C`／`U+200D`／`U+FEFF`）時回退既有預設佔位文字「U」；dashboard 既有 `avatarLabel` 角色字母覆寫行為不受影響。未新增或移除 FR/AC，僅澄清既有 FR-001「同一份 contract」語意，狀態維持 `spec-ready`。 |
| 2026-09-24 | `annotation-015` 更新至 v6.16.0（issue #907，OpenSpec change `pl-exception-disposal-screen-shell` 已 archive 為 `openspec/changes/archive/2026-09-24-pl-exception-disposal-screen-shell/`）：專案負責人之最終例外處置畫面改用例外池專屬外殼——左側清單只列該 `task_id × run_type` 的待處置例外項且與任務詳情頁例外池計數同源、進度以例外項為單位、不渲染自動儲存狀態列、每項就地呈現仲裁者與「兩者皆非」理由、`exclude_from_dataset` 帶危險樣式；修訂 FR-095、新增 AC-4.69 與 SC-011。正典依 issue #578／#596 先例留在 `specs/annotation/015-annotation-workspace/`，狀態自 `change-open` 回到 `in-progress`。 |
| 2026-09-24 | `foundation-002` 更新至 canonical spec v2.1.0（issue #906，PR #918，OpenSpec change `strengthen-path-map-cited-source-freshness` 已 archive 至 `openspec/changes/archive/2026-09-24-strengthen-path-map-cited-source-freshness/`）：freshness checker 先前只比對畫面清單指紋，內容過期仍會回 fresh；本版在指紋相符後追加 Stage 3 被指名來源判定。**MINOR**（只新增 MUST、無移除）：FR-004 追加受監看集合、FR-005 追加「全部即時重算且不得用任何時間戳」、FR-006 追加指紋不符優先結束與 Stage 3 逐項判定、FR-007 追加三個 stable rule ID；AC-2.2／AC-2.4 追加對應情境、AC-2.5 negative control 前提收斂、SC-003／SC-004 追加 Stage 3 要求。既有 v2.0.0 條文逐字保留為沿革。狀態維持 `in-progress`，分支欄與正典 frontmatter `功能分支` 同為 `feat/path-map-freshness-check`，不隨本次變更分支調整。 |
| 2026-09-23 | 維護者裁定 `foundation-001`、`foundation-002`、`task-management-014` 與 `annotation-015` 在正式系統完成前不應封存；四份正典分別移回各自的 `specs/<module>/` 目錄，STATUS 統一改為 `in-progress`，並同步 derived view、畫面清單與現行 traceability 路徑。歷史變更紀錄維持當時狀態，不回寫改動。 |
| 2026-09-23 | issue #868 apply PR #877 已以 merge commit `114f28cd` 合併：`annotation-015` v6.15.0 與 `task-management-014` v4.2.0 的 OpenSpec changes、prototype、正典回寫與驗證證據均已落地；兩份正典移回 `specs/_archive/015-annotation-workspace/` 與 `specs/_archive/014-task-detail/`，STATUS 改為 `archived`，derived Purpose 與 screen inventory manifest 同步指向封存路徑。 |
| 2026-09-23 | issue #868 進入 OpenSpec propose：以兩份 companion change 分別維護 annotation-015（`reserve-arbiters-from-review-assignment`）與 task-management-014（`validate-reviewer-arbiter-role-separation`）；兩份正典由封存路徑暫移回 active 模組路徑、狀態改為 `change-open`。本階段只建立提案與治理路徑，不提前改寫需求正文。 |
| 2026-09-19 | `annotation-015` 更新至 v6.8.0、`task-management-013` 更新至 v8.1.1（issue #811，OpenSpec change `split-bypass-answer-and-decision-wording` 已 archive 為 `openspec/changes/archive/2026-09-19-split-bypass-answer-and-decision-wording`，群組 1 PR #859（`7c9ab17e`）、群組 2 PR #860（`278e4872`））：「無法判定」同時是標記員答案值（`OutputAnswer.bypass`）與審核員決策值（`REVIEW_DECISIONS` 之 `bypass`），審核卡與仲裁 B 選項上兩者並排無從區辨；本版答案值維持 `無法判定 (Bypass)`，決策值改 `無法裁決`，兩套語彙各自以 `shared/sidebar.js` `BYPASS_WORDING` 為唯一來源（015 FR-092 新增 MUST、AC-3.60），FR-083／AC-3.47 鍵名定為 `toastReasonRequired`；013 FR-003j 英文文案同步（PATCH）。 |
| 2026-09-19 | `task-management-013` 更新至 v8.1.0 並由 `change-open` 改回 `in-progress`（issue #755，PR #853（`d3f806a8`），OpenSpec change `task-new-step1-field-role-hints` 已 archive 為 `openspec/changes/archive/2026-09-19-task-new-step1-field-role-hints/`）：新增 FR-002c-8、AC-1.6、AC-1.7、SC-002h；依該 change `tasks.md` 3.7 於 PR 合併後另行更新本列。 |
| 2026-09-19 | `annotation-015` 更新至 v6.7.0（issue #834，分支 `feat/834-trial-feedback-by-round`，OpenSpec change `annotation-dry-run-feedback-by-round` 已 archive 為 `openspec/changes/archive/2026-09-19-annotation-dry-run-feedback-by-round`）：FR-096 試標歷史回饋之揭露閘門由任務狀態改為逐回合判定——已結束回合於下一回合進行中及 `official_run_in_progress`／`completed` 後仍可見、進行中回合不揭露、無回合標記之提交於 `dry_run_in_progress` 時 fail closed、被修改筆數與占比逐回合分列，新增 AC-1.28。維護者裁定：試標已結束且零回饋時仍顯示「共 0 筆」摘要。兩份回合來源（`materializedRuns`／`trialRounds`）統一另由 issue #850 追蹤。 |
| 2026-09-19 | `task-management-014` 更新至 v4.1.1（issue #838，Lightweight Path，PATCH，分支 `fix/838-fr017-revision-note-gate`，正典位於 `specs/_archive/014-task-detail/`）：原型落實 FR-017 修訂紀錄阻擋。v4.0.0（issue #791）之 change `design.md`「範圍界線」將此阻擋列為範圍外，原型因此可不填修訂紀錄即自 `waiting_iaa_confirmation` 建立 R2 以後的回合；本版 `task-detail.html` 新增 `#trialRoundRevisionModal`，缺項逐欄提示並以與 FR-010t 相同的逐項清單阻擋，R1 豁免，修訂紀錄寫入新 `TrialRound`。規格條文未變（FR-017、FR-013、AC-3.12 不動），未新增或移除任何 FR／AC／SC 編號，不改動 `openspec/specs/` 衍生檢視。新增回歸 `issue-838-fr017-revision-note-gate.spec.ts`，並更新 `task-detail-stage-flow.spec.ts`、`issue-791-trial-round-from-waiting.spec.ts` 之 R2 建立步驟。 |
| 2026-09-19 | `annotation-015` 更新至 v6.6.0、`task-management-010` 更新至 v2.2.0、`dashboard-012` 更新至 v2.12.0（`chore/815-canon-writeback`，PR #844（`98258a80`），issue #815，OpenSpec change `retire-stale-review-demo-fixtures` 已 archive 為 `openspec/changes/archive/2026-09-19-retire-stale-review-demo-fixtures`）：退役只能由已廢止多數決規則產生的平手示範任務 T017，示範任務收斂為 T014–T016；新增 AC-6.12／AC-6.13 與 `scripts/check-demo-data-parity.sh` 一致性閘門。五條堆疊 PR：#829／#839／#841／#842／#844。Source-Verify 對衍生檢視新增之 24 條引用逐條 grep，0 MISSING。三份正典皆為活躍目錄，無需移入 `specs/_archive/`。衍生 issue #843（T015 `ofs-03` 登錄兩位審核員）。 |
| 2026-09-18 | `task-management-014` 重新封存：狀態由 `change-open` 轉回 `archived`，正典 spec 由 `specs/task-management/014-task-detail/` 移回 `specs/_archive/014-task-detail/`，`design/system/inventory-manifest.json` 的 `specs` 陣列同步改指封存路徑並重生 inventory；derived view 之正典版本註記更新為 v4.1.0 並補列 change `task-detail-iaa-precondition-and-override-scope` 之 FR-010o-1／FR-010o-4，`dataset/017` derived view 之正典版本註記由過期的 v2.2.2 更新為 v3.0.1 並補列 change `dataset-quality-entity-value-alignment`（維持 Source-Verify 逐項可 grep）。PR #840（`d9520f43`）合併後完成，依 #772／PR #802 先例。 |
| 2026-09-18 | `task-management-014` 更新至 v4.1.0、`dataset-017` 更新至 v3.0.1 並結束 `change-open`（`docs/783-iaa-precondition-017-values-on-main`，PR #840（`d9520f43`），issue #783，OpenSpec change `task-detail-iaa-precondition-and-override-scope` 與 `dataset-quality-entity-value-alignment` 已 archive 為 `2026-09-18-` 前綴同名目錄）：014 修訂 FR-010o-1、新增 FR-010o-4 與 `TrialRound.iaa_computation_status`（MINOR）；017 對齊 `SharedMetrics`／`risk_level` 實體與常數、FR-039 第 1 點補交叉引用（PATCH）。`dataset-017` 正典留在模組目錄，狀態回到 `in-progress`。 |
| 2026-09-18 | `task-management-014` 更新至 v4.0.0（MAJOR/BREAKING，`docs/791-trial-round-from-waiting`，PR #830（`96056e39`），issue #791，OpenSpec change `task-detail-trial-round-from-waiting` 已 archive 為 `2026-09-18-task-detail-trial-round-from-waiting`）：新增試標回合僅能自 `waiting_iaa_confirmation` 發起；FR-013 改寫、AC-3.12 前提改寫，新增 AC-3.14、AC-3.15、AC-3.16、SC-047；ADR-022 轉換表新增 `waiting_iaa_confirmation → dry_run_in_progress`。狀態維持 `change-open`：同一正典尚有 issue #783 change `task-detail-iaa-precondition-and-override-scope` 開立中，依 `ACTIVE_CHANGE_SPEC` 正典須留在模組目錄，待該 change 合併後再封存。FR-017 修訂紀錄阻擋另由 issue #838 追蹤。 |
| 2026-09-18 | issue #783 完成 `/opsx:propose`，拆為兩個 OpenSpec change（`scripts/check-sdd.sh` 之 `ACTIVE_CHANGE_SPEC` 要求一 change 恰對應一份正典）：`task-detail-iaa-precondition-and-override-scope`（`task-management-014`，第 1、2 點，疊在 issue #791 change 之上）與 `dataset-quality-entity-value-alignment`（`dataset-017`，第 3 點）。`dataset-017` 狀態由 `in-progress` 更新為 `change-open`，分支欄維持與正典 frontmatter 相同（先例 `3da42891`）；`task-management-014` 維持 `change-open`。規格條文於 propose 階段未變更。 |
| 2026-09-18 | `task-management-014` 完成 `/opsx:propose`，OpenSpec change `task-detail-trial-round-from-waiting` 已建立（issue #791：新增試標回合 R{n+1} 改為僅能自 `waiting_iaa_confirmation` 發起，新增 `waiting_iaa_confirmation → dry_run_in_progress` 轉換，`dry_run_in_progress` 期間不再顯示「新增試標回合」），狀態由 `archived` 更新為 `change-open`。propose 前置：正典 spec 由 `specs/_archive/014-task-detail/` 取回至 `specs/task-management/014-task-detail/`（`scripts/check-sdd.sh` 之 `ACTIVE_CHANGE_SPEC` 要求，issue #648），正典 frontmatter `功能分支` 同步更新為 `docs/791-trial-round-from-waiting`；規格條文於 propose 階段未變更。 |
| 2026-09-18 | `foundation-002` 封存：PR #831 合併後，狀態由 `review` 轉為 `archived`，正典 spec 由 `specs/foundation/002-user-path-map-freshness-check/` 移至 `specs/_archive/002-user-path-map-freshness-check/`，derived view `openspec/specs/foundation/002-user-path-map-freshness-check/spec.md` 內的正典路徑同步更新為 `specs/_archive/002-user-path-map-freshness-check/spec.md` v2.0.0（維持 Source-Verify 逐項可 grep）。issue #665 已關閉。 |
| 2026-09-18 | `foundation-002` 更新至 canonical spec v2.0.0，狀態由 `change-open` 轉為 `review`（`docs/665-path-map-fingerprint-amendment`，PR #831，issue #665，OpenSpec change `add-user-path-map-freshness-check` 已 archive 至 `openspec/changes/archive/2026-09-18-add-user-path-map-freshness-check/`）：依 2026-09-18 維護者裁定，Stage 2 時效判準由 Git commit 比對改為畫面清單指紋（`<head>` meta 記錄 `screen-inventory.md` 畫面／檢視 ID 清單之 sha256，checker 不呼叫 git），production gate 接上 `user-path-map-freshness` CI job 與 `CLAUDE.md` 本機指令。MAJOR：FR-004 監看範圍不再直接含 `design/prototype/pages/**`，AC-2.3 退役併入 AC-2.2（原文保留供追溯）；FR-005～FR-007、AC-2.1～AC-2.5、AC-3.2、SC-003～SC-005 的 blocked 標記解除；SC-006 與 Out of Scope 放行唯一的 `<head>` 指紋 meta。derived view `openspec/specs/foundation/002-user-path-map-freshness-check/spec.md` 由 archive 新建。 |
| 2026-09-18 | `annotation-015` 更新至 v6.5.0（`feat/818-block-empty-modify-answer`，PR #827，issue #818，OpenSpec change `block-empty-modify-review-answer` 已 archive）：審核員可對某 outKey 選 `修正`、填妥理由、清空答案後送出，一筆空的 `修正` 即進入儲存層，於資料層與 `無法判定` 同形（issue #810 仲裁版面誤判之根因，讀取端已由 PR #817 修正）。本版修寫入端：FR-083 送出驗證擴為三項條件，`reviewRowBlocker()` 新增 `'answer'` 回傳值，全部阻擋皆為空答案時 toast 用新鍵 `toastAnswerRequired`；新增 AC-3.59。MINOR：新增一則 AC 與一項送出驗證條件、不移除既有需求。 |
| 2026-09-18 | `annotation-015` 更新至 v6.4.0 並結束 `change-open`（`fix/627-rename-demo-sample-ids`，PR #828，issue #627 第 3 項，OpenSpec change `rename-misleading-review-sample-ids` 已 archive）：T016 兩個以 v5.0.0 已廢止中間狀態詞命名之樣本識別碼改名——`ofm-02-approved-interim` → `ofm-02-reviewer-accepts-a`、`ofm-03-modified-interim` → `ofm-03-awaiting-arbitration`；FR-044 新增約束（識別碼不得含 `approved`／`modified`、所有出現處須同批改名），AC-4.31、AC-4.36 引文同步。MINOR：新增一項規範性約束，未新增或移除任何 FR／AC 編號；AC-4.32、AC-4.37 沿革原文、已撤銷之 AC-4.47 與 Changelog 舊列逐字保留。 |
| 2026-09-18 | `task-management-014` 更新至 v3.3.1（issue #801，Lightweight Path，PATCH，正典位於 `specs/_archive/014-task-detail/`）：釐清 FR-021 第 (6) 項其一「無法重建」的必要欄位口徑。v3.3.0 原文要求快照具備「第 (3) 項與 FR-010i-2 所列任一必要欄位」，但 FR-010i-2 的列舉含 `scope_label`／`export_type`，而 issue #772 change 之 `design.md` D2／Q6 已定案本次不把兩者搬進快照（只影響匯出記錄表顯示、不影響檔案內容）——照字面讀，v3.3.0 之後每一筆紀錄都應停用「下載」，與 FR-021 本意矛盾。本版收斂為「重建檔案內容所需的欄位」，明列 FR-010i-2 中屬此範圍者為 `export_format`／`run_stage`／`submission_status`／`annotator_scope`，並明載 `scope_label`／`export_type` 不屬必要欄位。行為不變（`isArExportSnapshotComplete()` 既有檢查即為本版口徑），未新增或移除任何 FR／AC／SC 編號，FR-010i-2 與 FR-020 條文本身不動；依 #784／v6.3.1 先例，Lightweight Path 釐清不改動 `openspec/specs/` 衍生檢視。 |
| 2026-09-18 | `annotation-015` 更新至 v6.3.2、`dashboard-012` 更新至 v2.11.1（issue #627 第 7 項，Lightweight Path，分支 `fix/627-remove-unfinalized-summary-clause`）：審核員摘要移除「未達定稿門檻 {n} 個」一項。此為補 v5.0.0 的漏改而非移除需求——AC-1.24 早在該版就已刪除本項並留下註記，FR-076 第 1 點未同步，正典自此自相矛盾。三態模型下 `unfinalized = total - counts.finalized` 恆等於 `pending + disputed`，三者並列誘導讀者相加，且 `MIN_REVIEWERS_DEFAULT` 退場後「門檻」已無指涉對象。015 修訂 FR-076 第 1 點與 AC-1.22（三處引文字串＋第 3 個 `And` 子句）；012 修訂 FR-022，因其宣告 015 FR-076 為唯一來源卻逐字複述完整格式。`unfinalized` 欄位保留不動。原型僅 2 檔（`annotation-workspace.data.js` 移除 zh／en 兩個文案鍵與對應 `push()`、`dashboard.assignments.js` 一則註解），新增回歸 `issue-627-unfinalized-clause-removed.spec.ts`，6 個既有 spec 措辭斷言同步 retarget。**刻意排除**：AC-1.24 第 4 個 `And` 子句、`annotation-list.html` 一則註解、012 FR-021 的 `REVIEW_UNIT_ACTION_PRIORITY` 描述仍含 v5.0.0 已移除語彙，屬同源但不同項之殘留，已記錄於 issue #627。 |
| 2026-09-17 | `task-management-014` 重新封存：狀態由 `change-open` 轉回 `archived`，正典 spec 由 `specs/task-management/014-task-detail/` 移回 `specs/_archive/014-task-detail/`，`design/system/inventory-manifest.json` 的 `specs` 陣列同步改指封存路徑並重生 inventory，derived view 之正典版本註記更新為 v3.3.0 並補列 change `task-detail-export-history-redownload` 之 FR-021（維持 Source-Verify 逐項可 grep）。PR #799（`54736f60`）合併後完成，依 #742／PR #786 先例；後續追蹤 issue #800（篩選後零樣本不檢查切詞引擎）、#801（FR-021(6) 必要欄位措辭與 Q6 定案不一致）。 |
| 2026-09-17 | `task-management-014` 更新至 v3.3.0 並結束 `change-open`（`feat/772-export-history-redownload`，PR #799（`54736f60`），issue #772，OpenSpec change `task-detail-export-history-redownload` 已 archive 為 `2026-09-17-task-detail-export-history-redownload`）：新增 FR-021 匯出記錄重新下載依條件快照重建且不新增紀錄；新增 AC-1.14、AC-1.15、AC-1.16、SC-046，MINOR：只新增不修改既有條文。 |
| 2026-09-17 | `task-management-014` 完成 `/opsx:apply` 與 `/opsx:archive`：OpenSpec change `task-detail-export-history-redownload` 封存為 `openspec/changes/archive/2026-09-17-task-detail-export-history-redownload/`，正典回寫 v3.3.0（FR-021、AC-1.14–1.16、SC-046），狀態維持 `change-open` 至 PR 合併後重新封存。 |
| 2026-09-17 | `task-management-014` 完成 `/opsx:propose`，OpenSpec change `task-detail-export-history-redownload` 已建立（issue #772：匯出記錄表「下載」按鈕依該筆條件快照重新產檔，不讀取當前頁面篩選與對話框選項、不新增匯出記錄，`sequence_tagging` 重建檔須與原檔逐字元相同），狀態由 `archived` 更新為 `change-open`。propose 前置：正典 spec 由 `specs/_archive/014-task-detail/` 取回至 `specs/task-management/014-task-detail/`（`scripts/check-sdd.sh` 之 `ACTIVE_CHANGE_SPEC` 要求 change 開立期間正典位於模組目錄下，issue #648），正典 frontmatter `功能分支` 同步更新為 `feat/772-export-history-redownload`；規格條文於 propose 階段未變更。 |
| 2026-09-17 | `annotation-015` 更新至 v6.3.1（issue #784，Lightweight Path）：釐清 FR-072 第 1 點與 FR-073 第 2 點第 1 順位之「標記員尚無提交」口徑——issue 提議將不可處理之未提交單位排出快速審核候選，核對 prototype 後前提不成立（`listReviewUnits()` 僅列舉示範列，AC-3.38 閘門須無提交且無示範列才觸發，兩集合互斥），改以條文寫明 FR-044a 兩個 seed 來源皆缺之單位不在列舉範圍內；僅文件變更，未新增或移除任何 FR／AC 編號，新增回歸 `issue-784-enumerated-units-have-seed-source.spec.ts`；反方向缺口（有真實提交但無示範列）另開 issue #792 |
| 2026-09-17 | `annotation-015` 更新至 v6.3.0 並結束 `change-open`（`feat/766-finalized-card-remaining-cue`，PR #788，issue #766，OpenSpec change 已 archive 為 `2026-09-17-finalized-card-remaining-cue`）：唯讀定稿卡補上本任務剩餘可處理量——新增 **FR-100**（剩餘量取自與 `findNextActionableReviewUnit()` 共用之可處理單位清單，歸零時沿用 `list-no-actionable-notice` 之措辭並提供帶 `notice=no_actionable_review` 之回清單連結）、**AC-3.57**、**AC-3.58**、**SC-004Z**；純 ADDED，未修訂既有條文。正典位於 `specs/annotation/`、不曾移入 `specs/_archive/`，故狀態回到 `in-progress` 而非 `archived` |
| 2026-09-17 | `task-management-014` 重新封存：狀態由 `change-open` 轉回 `archived`，正典 spec 由 `specs/task-management/014-task-detail/` 移回 `specs/_archive/014-task-detail/`，`design/system/inventory-manifest.json` 的 `specs` 陣列同步改指封存路徑並重生 inventory，derived view 之正典版本註記更新為 v3.2.0（維持 Source-Verify 逐項可 grep）。PR #773–#777（最後 `c5809bbf`）合併後完成，依 #688／#726／PR #769 先例。 |
| 2026-09-16 | `task-management-014` 更新至 v3.2.0 並結束 `change-open`（`feat/742-seq-tagging-export-dialog`，PR #773–#777（最後 `c5809bbf`），issue #742，OpenSpec change `task-detail-seq-tagging-export-dialog` 已 archive 為 `2026-09-16-task-detail-seq-tagging-export-dialog`）：新增 FR-020 `sequence_tagging` 匯出對話框與序列匯出欄位——承接 `dataset/017` v3.0.0 之 FR-041／FR-042 匯出推導契約，新增方案／詞元單位選擇器、詞級對齊擴張摘要、tokenizer 版本缺漏阻擋；新增 AC-1.10、AC-1.11、AC-1.12、AC-1.13、SC-045，MINOR：只新增不修改既有條文。 |
| 2026-09-16 | `task-management-014` 完成 `/opsx:propose`，OpenSpec change `task-detail-seq-tagging-export-dialog` 已建立（issue #742：`sequence_tagging` 匯出對話框，承接 `dataset/017` v3.0.0 使用者故事 5 之 UI 條款——標註方案／詞元單位選擇器、詞級對齊擴張摘要、`tokenizer.version` 缺漏阻擋），狀態由 `archived` 更新為 `change-open`。propose 前置：正典 spec 由 `specs/_archive/014-task-detail/` 取回至 `specs/task-management/014-task-detail/`（`scripts/check-sdd.sh` 之 `ACTIVE_CHANGE_SPEC` 要求 change 開立期間正典位於模組目錄下，issue #648），正典 frontmatter `功能分支` 同步更新為 `feat/742-seq-tagging-export-dialog`；規格條文於 propose 階段未變更。 |
| 2026-09-16 | `task-management-013` 更新至 v8.0.0 並結束 `change-open`（`feat/remove-task-new-step1-preset`，issue #724，OpenSpec change 已 archive 為 `2026-09-16-remove-task-new-step1-type-preset`）：移除 task-new Step 1 的常用組合一鍵預設。該功能 2026-09-13 隨 v7.1.0 上線，上線後檢視版面判定投報率不成立——預設清單僅 1 筆，卻佔據 Step 1「任務類型」欄位最上方一整列，並在三組 chip 之外多加一層使用者須先理解的介面概念，只有恰好建立該一種任務型態的使用者省下 2 次點擊。破壞性移除 FR-002f、AC-1.4、AC-1.5、SC-002g 與 1 條邊界情況；FR-002／FR-002a–FR-002e 之選擇語意與 `validateStep1()` 不變。issue #724 之方向 ②（欄位角色批次動作／依欄名推測）與 ③（必填項合理預設）仍由 issue #755 追蹤。 |
| 2026-09-15 | `task-management-014` 更新至 v3.1.0 並結束 `change-open`（`feat/726-task-detail-url-view-state`，PR #768，issue #726，OpenSpec change `task-detail-url-view-state` 已 archive 為 `2026-09-15-task-detail-url-view-state`）：新增 FR-019（頁籤與清單檢視狀態的網址同步，15 個參數、`history.replaceState()`、tab 前綴命名空間 `ap_`／`ar_`／`mm_`／`wl_`、角色剪枝），MINOR：只新增不修改既有條文。 |
| 2026-09-15 | `task-management-014` 重新封存：狀態由 `change-open` 轉回 `archived`，正典 spec 由 `specs/task-management/014-task-detail/` 移回 `specs/_archive/014-task-detail/`，`design/system/inventory-manifest.json` 的 `specs` 陣列同步改指封存路徑並重生 inventory，derived view 之正典版本註記更新為 v3.1.0（維持 Source-Verify 逐項可 grep）。PR #768 合併後完成，依 #688／PR #706 先例。 |
| 2026-09-14 | `annotation-015` 更新至 v6.2.0 並結束 `change-open`（`docs/issue-719-review-autoadvance`，PR #751，issue #719，OpenSpec change 已 archive 為 `2026-09-10-review-submit-auto-advance`）：審核端補上送出後自動前進——新增 **FR-099**（送出審核或仲裁後導向依 FR-073 推導之下一個可處理審核單位，無可處理單位時導回清單並帶 `notice=no_actionable_review`；定稿之送出依 AC-3.39／FR-053 仍須停留）、**AC-3.55**、**AC-3.56**、**SC-004Y**；同版修訂 **FR-073** 第 2 點之第 1 順位，補上原缺漏的 FR-093 指派條件（`findNextActionableReviewUnit()` 簽章不變而行為改變，第 2 順位之仲裁刻意不套用該條件），並修訂 FR-099 第 8 點與本版 Changelog 中「不修改任何既有條文」之錯誤陳述，未新增或移除任何 FR／AC 編號 |
| 2026-09-13 | `shared-008` 更新至 v1.5.0 並結束 `change-open`（PR #757，issue #725，OpenSpec change `admin-role-settings-nav-shortcut` 已 archive 為 `2026-09-13-admin-role-settings-nav-shortcut`）：新增使用者故事 8、FR-019 群（FR-019～FR-019E）、SC-012 群（SC-012～SC-012B）——`super_admin` 側欄「系統管理」在 Desktop 未收合狀態下提供可展開次選單（使用者管理／角色設定），一次點擊直達 `role-settings`；次選單子項不計入既有 FR-002／FR-003A／SC-003 之 L0 導覽項清單與計數，兩者文字逐字不變。維護者已就「新增側欄一級項目 vs. 展開次選單」的架構衝突裁示採用後者。同一 PR 補正 CLAUDE.md 的 Module names 清單缺列 `shared` 的既有漏項（`specs/shared/` 自 shared-001 起即存在，非新增模組）。 |
| 2026-09-13 | `shared-008` 完成 `/opsx:propose`，OpenSpec change `admin-role-settings-nav-shortcut` 已建立（issue #725：`super_admin` 調整角色權限路徑過長，維護者裁示採方案 (b)——側欄「系統管理」改為可展開次選單直達 `role-settings`，不新增 L0 項目），狀態由 `spec-ready` 更新為 `change-open`。propose 前置：spec 補齊缺漏的 `## 功能目標` 標題並升版至 v1.4.4（Project SDD lint ratchet，規格條文未變），`scripts/sdd-lint-baseline.txt` 同步移除對應豁免項。 |
| 2026-09-13 | `task-management-013` 更新至 v7.1.0 並結束 `change-open`（`feat/task-new-step1-click-reduction`，issue #724，OpenSpec change 已 archive 為 `2026-09-13-task-new-step1-type-preset`）：task-new Step 1 任務類型選擇新增常用組合一鍵預設——新增 **FR-002f**（registry／taxonomy 驅動的一鍵預設按鈕，套用效果與逐一點選三組 chip 完全等價，三段式選擇器維持完整可見與可個別再調整，核心選擇流程不得新增依 key 硬編分支）與 **AC-1.4**、**AC-1.5**、**SC-002g**，新增 1 條邊界情況；不修改任何既有 FR/AC/SC 條文，不新增或變更提交 payload 欄位，`validateStep1()` 既有必填判斷不變。本版僅承接 issue #724 三個方向中的方向 ①（型態選擇一鍵預設）；方向 ②（資料集欄位角色批次動作或依欄名自動推測初值）與方向 ③（`validateStep1()` 必填項是否有可免點的合理預設值）留待 issue #755 追蹤。分支欄同時校正正典 frontmatter 之既有漂移（原 `fix/issue-659-sequence-tagging-overlap-feedback` 為該分支已合併後未同步之舊值）。 |
| 2026-09-10 | `annotation-015` 更新至 v6.1.1（issue #748，Lightweight Path）：補正 AC-1.23 之 v5.0.0 回寫遺漏——v5.0.0（issue #596）已廢止 FR-073 的 `REVIEW_UNIT_ACTION_PRIORITY` 第 3 順位，但對應 AC-1.23 未同步修訂，仍保留 T016 範例的「未達門檻 2」計數與「未參與部分未達門檻單位的 `reviewer_li` 則導向 `ofm-02-approved-interim`」子句——兩者描述已廢止的第 3 順位行為，於現行三態模型下永遠不可能成立。本次移除上述計數與子句，修訂 AC-1.23 本身之措辭，但未新增或移除任何 FR/AC 編號、FR-073 條文本身不動。狀態維持 `in-progress`，正典 spec 位置不變。 |
| 2026-09-10 | `dataset-017` 更新至 v3.0.0 並結束 `change-open`（`feat/issue-581-c3-archive`，PR #746，issue #581，OpenSpec change 已 archive 為 `2026-09-09-seq-tagging-span-export-metrics`）：`sequence_tagging` 的匯出契約與 IAA 主指標自 token 座標系遷移至 span 模型——**新增 FR-041**（匯出層 BIO／BIOES／IOB2 決定性推導契約，SSoT；常數 `EXPORT_TAGGING_SCHEMES`／`EXPORT_DEFAULT_TAGGING_SCHEME`／`EXPORT_TOKEN_UNITS`／`EXPORT_DEFAULT_TOKEN_UNIT`）、**FR-042**（詞級 tokenizer metadata 與對齊擴張報告；常數 `SPAN_TOKEN_ALIGNMENT_MODE = expand`，擴張只發生於匯出產物且 MUST NOT 回寫 `spans[]`）、**FR-043**（未校準門檻型別的中性呈現；常數 `IAA_UNCALIBRATED_TYPES = sequence_tagging`）；**修訂** FR-009L（統計母體改 span 集合）、FR-012L（主指標改 Krippendorff 單位化 α，`IAA_THRESHOLD_TOKEN = 0.75` 與 Token-level Alpha 一併廢止且識別名稱不重用）、FR-013、FR-024A、FR-035（分歧度改 pairwise span F1 strict）、FR-036（一致率改與合併聚合參考值的 F1）、FR-039 第 1 與第 4 點；同步更新 `OUTPUT_TYPE_IAA_REGISTRY` 表格列、規格常數區逐型條列、兩處介面定義、故事 2 與故事 3 的行為規則區塊與三個實體欄位列舉。Gate 4 的 Source-Verify 逐條 grep 另抓出一處回寫缺口——`AC-3.18` 只存在於 delta 的 `#### Scenario:`，四個閘門皆不報錯，已於 `8a4e03d3` 補回。匯出對話框 UI 由 issue #742 承接（裁決 D3、H）；`entity_recognition` 主指標與門檻不動（裁決 D2）。正典 spec 依 issue #578／#596 先例留在 `specs/dataset/`，狀態回到 `in-progress` 而非 `archived`。 |
| 2026-09-09 | `annotation-015` 更新至 v6.1.0 並結束 `change-open`（`feat/issue-590-archive-writeback`，PR #740，issue #590，OpenSpec change 已 archive 為 `2026-09-09-carry-relation-span-offsets`）：`relation_identification` 之答案結構補上主體與客體起訖——新增 **FR-098**（引擎快照 `previewTriples` 攜帶 `subjStart`／`subjEnd`／`objStart`／`objEnd`；CompactAnswer 自三鍵擴為八鍵含 `relType` 且序列化與回填兩端對稱；span label 為 `role@relationKey`；逐快照對回退並明文豁免「某一側完全無該輸出鍵答案」之情形；**禁止**以答案字串回原文做字串比對推得 offset 之硬規則）與 **AC-2.22**、**AC-2.23**、**SC-004X**；**修訂 FR-087** 之已知落差，`relation_identification` 自「整型別暫以純值比對遞補」收斂為「依來源形狀分流」——工作區互動標記與物件形狀資料集匯入兩種來源走逐實體差異，gold 純字串與內建示範串接兩種來源維持純值比對並轉由 issue #738 追蹤。索引鍵將關係型別編入 label 只降低碰撞率、殘留碰撞已於 FR-098 第 5 點明載，未對外宣稱已完全防碰撞。同次回寫依 issue #581 裁決 D5 清除 `## 規格常數` 中 v6.0.0 自 payload 移除 `scheme` 時漏刪的孤兒序列標註標記方案列舉宣告（全檔無條文引用）。正典 spec 依 issue #578／#596 先例留在 `specs/annotation/`，故狀態為 `in-progress` 而非 `archived`。 |
| 2026-09-07 | `task-management-014` 更新至 v3.0.0（`docs/issue-688-archive-014-review-model`，issue #688，OpenSpec change `align-014-review-model` 完成 archive）：014 正典追上 `annotation-015` v5.0.0 之單人接力審核模型，解除兩份正典間六處矛盾。**移除**：`REVIEW_ASSIGNMENT_MODES`／`MIN_REVIEWERS_RULE` 兩個規格常數、`TaskDetail` 四欄位（`min_reviewers`／`review_assignment_mode`／`agreement_auto_finalize`／`arbitration_enabled`）、Overview 同名四個檢視/編輯欄位、成員管理審核指派區塊三組操作按鈕（自動補齊／指派…／分派給仲裁者）。**修訂**：`AR_REVIEW_STATUS` 五態改三態（移除 `approved`／`modified` 中繼態）；審核設定改為 `reviewer_ids`／`arbiter_ids` 兩份勾選名冊，新增 `REVIEWER_ID_FORMAT`（不透明 user id，取值來源 `TaskMembership.user_id`，形狀比照 015 `REVIEWER_ROSTER`，解除 PR #683／issue #617 卡住的身分格式歧義）；`ARBITER_CANDIDATE_RULE` 加 `can_arbitrate = true`；FR-008b 結案前置條件、FR-010t 發布前人數檢查同步改版。**新增**：FR-018 最終例外池（`annotation-progress` 頁籤，`project_leader` 逐筆收尾仲裁「兩者皆非」之爭議項）、規格常數 `EXCEPTION_POOL_ACTIONS`、AC-3.13、SC-043。實作分兩個 PR 群組完成——群組 1（PR #701，審核員身分格式統一）、群組 2（PR #704，最終例外池入口與結案閘門）；本次為群組 3（archive 與正典回寫），OpenSpec change 已移至 `openspec/changes/archive/2026-09-07-align-014-review-model/`。**本次 Source-Verify 額外修正之既有缺陷**：delta 原引用之 `SC-041` 與正典既有（issue #491/#489/#492）之 `SC-041`／`SC-042` 衝突，改配 `SC-043`；PR #701 誤將 `2.11.3` Changelog 列插入「使用者流程與導頁」步驟表（4 欄表格寫入 3 欄內容）已移正。PR #706 合併後執行封存收尾，詳見下一列。 |
| 2026-09-07 | `task-management-014` 封存：狀態由 `in-progress` 轉為 `archived`，正典 spec 由 `specs/task-management/014-task-detail/` 移至 `specs/_archive/014-task-detail/`，derived view `openspec/specs/task-management/014-task-detail/spec.md` 內的正典路徑同步更新為 `specs/_archive/014-task-detail/spec.md` v3.0.0（維持 Source-Verify 逐項可 grep）。PR #706 合併後完成，issue #688 可關閉。 |
| 2026-09-04 | `annotation-015` 更新至 v6.0.0 並結束 `change-open`（`feat/issue-581-span-preview`，issue #581，OpenSpec change 已 archive 為 `2026-09-04-seq-tagging-span-workspace`）：`sequence_tagging` 標記工作區自 Token 網格＋完整 tag 點選破壞性改為原始文本拖曳圈選，資料形狀為半開區間 `spans[]`（`{ start, end, label }`）——修訂 FR-024A（單一圈選面、相交禁止而相鄰允許、Token 網格與 `B-`／`I-`／`E-`／`S-` 按鈕列整組移除）、FR-024A-1（`snap_unit` 只影響落點不影響儲存值，詞界改用前端 `Intl.Segmenter`，原 ADR-031「後端權威 Token 邊界」契約整組作廢；新增規則：缺 `Intl.Segmenter` 而降級的標註者，其答案 MUST 照常納入 IAA、共識與差異比對，不得標記為可疑、降權、排除或事後對齊詞界）、FR-024A-2（改逐 span 合法性驗證，移除「標記 tag 數量必須等於 Token 數量」硬約束與其邊界情境）、FR-024A-3（payload 改 `spans[]`＋`snap_unit`，移除 `tokens[]`／`tags[]`／`scheme`／`unit`，BIO 不再出現於 payload）、FR-052（`sequence_tagging` 由逐 token 位置比對改為集合比對，CompactAnswer 形狀改 `{ text, label, start, end }`、合併鍵 `start + end + label`、`(start, end)` 為權威而 `text` 僅供呈現）、FR-024L（統計主體由帶前綴 tag 改為標籤類型，n 字實體計為 1）；`SEQ_MAJORITY_INVALID_BIO_FALLBACK` 標為正式廢止且 ID 不重用，SC-008 之「Token 網格」改為「span 圈選面」；措辭同步（Lightweight Path）`task-management-014` v2.11.2；正典 spec 依 issue #578／#596 先例留在 `specs/annotation/`；BIO 序列改由 `dataset/017` 匯出層自 `spans[]` 決定性推導（change ③ 待辦）。 |
| 2026-09-04 | `task-management-013` 更新至 v7.0.0 並結束 `change-open`（`feat/issue-581-span-preview`，issue #581，OpenSpec change 已 archive 為 `2026-09-04-seq-tagging-span-config`）：`sequence_tagging` 的設定契約由「Token 網格＋`tagging_scheme`」改為 `entities`／`snap_unit`／`allow_bypass`，標記值改為字元 offset 的半開區間 `spans[]`；破壞性移除 v6.2.0～v6.4.0 的 BIO／BIOES／IOB2／SINGLE 方案、`tokenization` 契約、Token 網格與數量一致性阻擋，BIO 改由 `dataset/017` 匯出層自 span 推導；`entity_recognition` 改為共用同一套 `snap_unit` 與圈選預覽元件，兩型別的唯一差異為 `allow_overlapping` 是否可設定；改寫 SC-003x 並新增 SC-003y／SC-003z／SC-003aa／SC-003ab，另將 FR-003d-1 一致性傳播至 FR-003g-5；ADR-031 轉為 Superseded。`annotation-015` 的 consumer 端由同 issue 的 change `seq-tagging-span-workspace` 承接，`dataset/017` 留待第三個 change。 |
| 2026-09-04 | `foundation-001` 封存：狀態由 `review` 轉為 `archived`，正典 spec 由 `specs/foundation/001-project-sdd-lint/` 移回 `specs/_archive/001-project-sdd-lint/`，derived view `openspec/specs/foundation/001-project-sdd-lint/spec.md` 的正典版本號同步更新為 v1.2.0（維持 Source-Verify 逐項可 grep）。PR #658 合併後另批處理，因合併前正典需留在非封存路徑以支撐 open change 的 `ACTIVE_CHANGE_SPEC` 檢查。 |
| 2026-09-04 | `task-management-013` 完成 `/opsx:propose`，OpenSpec change `seq-tagging-span-config` 已建立（issue #581：`sequence_tagging` 由 token 座標系改為拖曳圈選 + 字元 offset `spans[]`），狀態由 `in-progress` 更新為 `change-open`；branch 欄位校正為正典 frontmatter 的 `feat/task-list-output-types`（先前誤填 v6.9.3 的暫時測試分支 `feat/task-new-wizard-draft-warning`）。同日 v6.9.6 為 40 條驗收情境配發 AC-N.N 穩定 ID（PR #637），為本 change 的前置條件。 |
| 2026-09-03 | `annotation-015` 更新至 v5.0.0 並結束 change-open（PR #629；issue #596，OpenSpec change `2026-09-01-single-owner-review-relay` 已 archive）：審核模型改為單一負責人接力（BREAKING）——每個審核單位恰好一位系統指派的審核員（FR-093，`dry_run` per_sample／`official_run` per_unit），決策收斂為 `approve | modify | bypass`（FR-092，`reject` 移除、`modify` 不即時生效而是使該單位進入爭議），`REVIEW_UNIT_STATUS` 自五態收斂為三態（待審／爭議中／已定稿，移除 `approved`／`modified` 中間態與審核員人數門檻），仲裁結果定為 `adopt_a | adopt_b | reject`（FR-061），例外池動作定為 `adopt_annotator | adopt_reviewer | custom_answer | exclude_from_dataset`（FR-095，`custom_answer` 僅 `official_run`）；新增 FR-092 ~ FR-097，整組移除 FR-014I／FR-069／FR-074／FR-085（依本檔慣例保留墓碑條目、ID 不重用），`HISTORY_ACTIONS` 自七值擴為九值（`rejected` 移除，v5.0.0 前既有事件原樣保留）；狀態由 `change-open` 轉回 `spec-ready`，正典 spec 依 issue #578 先例留在 `specs/annotation/` 未搬 `specs/_archive/`；衍生 issue #626（產品端殘留舊詞彙）、#627（遷移程式碼債）、#628（測試護欄） |
| 2026-09-01 | `foundation-001` 封存：狀態由 `review` 轉為 `archived`，正典 spec 由 `specs/foundation/001-project-sdd-lint/` 移至 `specs/_archive/001-project-sdd-lint/`，derived view `openspec/specs/foundation/001-project-sdd-lint/spec.md` 內的正典路徑與版本號同步更新為 `specs/_archive/001-project-sdd-lint/spec.md` v1.1.3（維持 Source-Verify 逐項可 grep）。依維護者指示於 PR #598 內完成，未待合併後另批處理。 |
| 2026-09-01 | `foundation-001` 更新至 canonical spec v1.1.3（PR #598；issue #375，OpenSpec change `implement-project-sdd-lint`）：獨立 `sdd-lint` CI job（`Project SDD Lint`，只 checkout + 執行 `scripts/check-sdd.sh`）與 `CLAUDE.md` 本機指令上線，change 已 archive 至 `openspec/changes/archive/2026-09-01-implement-project-sdd-lint`，derived view `openspec/specs/foundation/001-project-sdd-lint/spec.md` 首次產生（4 條需求）。同批修正兩項會讓新 gate 失效的問題：main 上 `design/system/screen-inventory.md` 自 `a422e1f2`／PR #593 起即 stale；`gen-screen-inventory.mjs` 以 `--format=%h` 取 prototype 來源 commit，git 動態 abbreviation 長度隨本機 object database 成長（本機 repack 後由 7 碼變 8 碼），使 freshness 的 byte-for-byte 比對不可重現——改讀 `%H` 並固定截 12 碼，FR-009 補上該可重現性依賴（澄清，未新增／移除需求）。狀態由 `change-open` 轉為 `review`。 |
| 2026-08-31 | `annotation-015` 更新至 v4.61.0（PR #591、#593；issue #578，OpenSpec change `enrich-annotation-history`）：標記／審核歷程事件模型擴充，新增 FR-086 ~ FR-091 與 AC-1.25／AC-2.15 ~ AC-2.21／AC-3.49／AC-3.50／AC-4.51，修訂 FR-016B 與關鍵實體 `AnnotationHistoryItem`。change 已 archive 至 `openspec/changes/archive/2026-08-31-enrich-annotation-history`，衍生檢視 `openspec/specs/annotation/015-annotation-workspace/spec.md` 自 8 條增至 14 條需求。Source-Verify 對 58 處引用逐條 grep 抓出 10 項缺陷並於 archive 前全數修正——其中三項會污染正典：delta 誤稱「沿用 FR-051 既有的 per-outKey 決策」（FR-051 全文不含 `outKey`，其定義為審核單位層級的 `REVIEW_UNIT_DIMENSIONS`，改引 FR-014B／FR-044／FR-052）、關鍵實體 `action` 列舉仍引已廢止之 FR-043 且列有從無產生點的 `approved`、FR-090 誤稱跨標記員隔離為既有條文（正典查無，改記為本版新確立、理據引 Constitution III Data Fairness）。衍生 issue #582、#583、#590。 |
| 2026-08-30 | `annotation-015` 更新至 v4.60.0（PR #573；issue #572，Lightweight Path）：審核單位脈絡橫幅 `ws-review-unit-context` 移除卡片外框（`.rv-unit-context` 之 background／border／border-radius／padding）與狀態 pill 的 `目前：`／`Now: ` 前綴（v4.50.0、issue #525 PR-B 引入）；撤銷 FR-064 第 7 點第 3 項、修訂第 4 項與 AC-4.27／AC-4.32／AC-4.39；`aria-label`／`data-terminal`／null 分支 `尚無標記提交`／抽屜狀態軌 `.review-track-marker` 皆不變。原型：`annotation-workspace.html`、`annotation-workspace.config.js`（刪除 `unitStateNowTpl`）；新增 `issue-572-banner-frame-and-prefix.spec.ts`，10 個既有 spec 前綴斷言改為裸狀態詞。 |
| 2026-08-30 | `annotation-015` 更新至 v4.59.1／v4.59.2（PR #564、#570；issue #563、#568，皆 Lightweight Path）：v4.59.1 修正 reviewer 視圖 `送出審核` 因左側自動儲存指示隱藏、`space-between` 只剩單一子元素而落到操作列左側，`.action-bar-right` 加 `margin-left: auto`；v4.59.2 以審核員角度全面檢查送出類按鈕位置後，將唯一不一致的 `送出仲裁` 自仲裁卡內移入底部固定操作列（`#wsArbitrationSubmitBtn`，testid 沿用 `ws-arbitration-submit`），`renderReviewer()` 進入先隱藏、僅有未裁定爭議項時顯示，點擊邏輯抽為 `handleArbitrationSubmit()`；兩版皆不新增／不移除任何 FR／AC，僅澄清 §頁面結構「中欄（下）：底部操作列」。本表先前未同步 v4.59.1，本列一併補記。 |
| 2026-08-30 | `annotation-015` 更新至 v4.59.0（PR #565；issue #562，Lightweight Path）：移除審核工作區橫幅下方的角色相依行動提示，撤銷 v4.57.0 之 FR-084、AC-4.47～AC-4.50，修訂 FR-064 第 7 點第 6 項；原型刪除 `buildReviewActionHint()`、`actionHint*` i18n 鍵與 `.rv-action-hint` 樣式，測試以 `issue-562-review-action-hint-removed.spec.ts` 取代 `issue-526-reviewer-action-hint.spec.ts`。 |
| 2026-08-29 | `annotation-015` 更新至 v4.56.0／v4.57.0／v4.58.0（PR #558、#560、#559；issue #557、#526、#552）：reviewer 左欄審核單位項目移除尾端重複樣本 ID（Lightweight Path）；依審核單位狀態 × Reviewer 身分顯示行動提示（OpenSpec change `reviewer-action-hint`，新增 FR-084、AC-4.47～AC-4.50）；退回強制填寫理由並於重標待辦顯示給標記員（OpenSpec change `reject-reason-required`，新增 FR-085、AC-2.14、AC-3.48，修訂 FR-016A／FR-014I／FR-070／FR-083）。兩個 OpenSpec change 皆已 archive 至 `openspec/changes/archive/2026-08-29-*`，derived view `openspec/specs/annotation/015-annotation-workspace/spec.md` 首次產生。 |
| 2026-08-27 | `dashboard-012` 更新至 v2.9.0（`feat/issue-510-remove-demo-shortcut`，issue #510）：撤銷 FR-009E／SC-027，移除 PL Dashboard 任務列表上方的「審核流程示範」快捷分類按鈕。issue #404 的立論為該批示範任務自儀表板「或其『查看全部』入口」皆無法自然發現，複查確認後半不成立——`plViewAllBtn` 呼叫 `openTaskList('project_leader')` 不帶 `keyword`，導向未篩選全量清單（預設 `limit: 20` > 17 筆種子），T014–T017 本就完整可見；按鈕實效僅為省去一次關鍵字輸入，不足以支撐其版面成本。移除原型 `renderDemoShortcuts()`、T014–T017 的 `demoCategory` 與 `demoCategories` 目錄、`dashboard-pl-review-flow-demo-entry.spec.ts`；T014–T017 任務資料保留（仍支撐 FR-011D／FR-011E／FR-020／FR-021／FR-022）。新測試 `issue-510-demo-shortcut-removed.spec.ts` 另將移除前提本身釘為迴歸守衛。 |
| 2026-08-27 | `foundation-001` 更新至 canonical spec v1.1.2：Stage 3 security remediation 將 inventory generator 限於 canonical resolved checker／target 同一 trust root；foreign explicit root 固定以 `INVENTORY_CHECK_CONFIG`／exit `2` 拒絕且不執行 hostile generator／產生 marker。動態 scanned pathname 在進入 newline/text/TSV flow 前以 NUL-safe preflight 拒絕 ASCII／locale-independent control character，固定 `SCANNER_CONFIG`、安全 path `.` 與 exit `2`。維持 `change-open`、`feat/issue-375-sdd-lint`、六個 truthful Issue handoff D 項目與六個 logical archive artifacts；Stage 3 Medium checkout persisted credentials 為 non-blocking，CI 不變。 |
| 2026-08-27 | `foundation-001` 更新至 canonical spec v1.1.1：Stage 2 誠實交接修正將 SC-007 的合併後 Issue #375 交接收斂為六個已交付 D 項目（正典標題、STATUS/stage、Source-Verify、task 單檔／例外、assignee／file ownership、design inventory freshness）。複合 retired-path/command D 項目與 combined acceptance 維持未勾選並延期，待獨立取得 ADR-034/path authority 並完成 named filesystem-path QA Red 與 production Green；不接受 ADR-034，亦不修改執行期程式碼。 |
| 2026-08-26 | `foundation-001` 更新至 canonical spec v1.1.0：Project SDD Lint 新增 FR-009、AC-4.1–AC-4.3、SC-006–SC-007，將 generated `design/system/screen-inventory.md` freshness 升為 exact-sentinel blocking contract（stale exit `1`；不可執行、sentinel-less 或其他 configuration result exit `2`）。本 integration branch 合併 upstream source additions 後，以既有 generator 刷新該 generated view 並驗證 `--check` current；generator／manifest implementation 與其他 inventory views 不在此變更範圍。 |
| 2026-08-26 | `foundation-001` 完成 `/opsx:propose`，OpenSpec change `implement-project-sdd-lint` 已建立，狀態由 `spec-ready` 更新為 `change-open`；branch 維持 `feat/issue-375-sdd-lint`。 |
| 2026-08-26 | 新增 `foundation-001` Project SDD Lint canonical spec v1.0.0，狀態設為 `spec-ready`；範圍為 ratchet baseline、task/Source-Verify/retired guidance checks 與獨立 CI gate。 |
| 2026-08-26 | `task-management-014` 更新至 v2.10.5（`feat/task-detail-reviewer-guideline-t014-t017`，issue #405）：修正 T014–T017 提供給審核員的指引內容從缺——`task-detail.data.js` 的四個審核流程示範任務 profile 原未設定 `reviewerGuidelineText`，Overview「提供給審核員」卡片全數落回共用空狀態，即使四者各自示範不同審核情境（dry_run 共識仲裁／單一審核員核可／三審核員多數決收斂／兩審核員平手）。比照 issue #395 `forceShowGuideline` 機制，`resetTaskData()` 新增一行泛用讀取（不含 task_id 分支），四任務各自 seed 對應審核情境的指引文字（含共用的 single_label 情感三分類判準）。**規格條文未變**（FR-014f-1 雙角色結構本已涵蓋，僅補齊 seed 資料）。新增回歸測試 `issue-405-reviewer-guideline-t014-t017.spec.ts`。 |
| 2026-08-26 | `dashboard-012` 更新至 v2.5.0（`feat/dashboard-review-demo-entry`，issue #404）：PL Dashboard 任務列表新增資料驅動的「示範任務快捷分類」入口——審核流程示範任務（T014–T017）先前只能透過標記員／審核員清單或已知在 `/task-list` 搜尋「審核流程示範」關鍵字才能發現，PL 場景無自然入口。`dashboard.data.js` 為任務新增泛用 `demoCategory` 標記與對應 `demoCategories` 目錄（比照既有 `outputTypes` 目錄模式），任務列表區塊依此動態渲染快捷按鈕，點擊導向 `/task-list?task_role=project_leader&keyword=<標籤>`；渲染邏輯不檢查任何具體 `task_id`（Generalization-First）。新增 FR-009E、SC-027。 |
| 2026-08-25 | `foundation-000` 由 `spec-ready` 改標 `done`，spec 更新至 v1.12.4：OpenSpec change `implement-foundation-core`（issue #356 Phase 4 pilot）已完整跑完 propose → apply → archive，Foundation-Core 範圍（plan.md v2.0.0 的 F-01–F-10、F-13、F-16、F-18）由 9 個 stacked PR（#374、#378、#381、#379、#388、#389、#390、#391、#392）落地，收尾與 archive 回寫為 PR #412。**無 FR/SC 新增、移除或措辭變更**——為實作回寫，非需求變更；三項實作與正典文字的落差（FR-021 實作在所有環境無條件拒絕萬用字元、SC-020 第二子句延後至 account-001、SC-045 的 OpenAPI export / type generation 子句歸屬 FR-071／SC-018 未實作）依維護者裁決記錄於正典 spec 的 Changelog 而不改動需求原文。**尚未執行封存**：本 spec 為全 repo 以路徑引用的跨模組長期基線，且 F-17 Observability 與 Celery 相關需求依 plan.md 延後、尚未實作，是否移入 `specs/_archive/` 待維護者裁決。 |
| 2026-08-25 | `foundation-000` 更新至 spec v1.12.3：SC-002 的驗證指令由 `uv run mypy app/ --strict` 校準為 `uv run mypy .`，與 `.github/workflows/ci.yml:122`、testing-constitution XII/XIII、backend-constitution 及 AGENTS.md 既有的寫法一致。原措辭範圍過窄（只檢查 `app/`，漏掉 `tests/`），在 foundation-core BE2 群組造成一次 CI 紅燈。同批同步 CLAUDE.md 驗證矩陣、`senior-backend`／`senior-code-reviewer` agent 定義與 tasks-template v1.22.5；CLAUDE.md 另補上 CI 已在跑但未列入的 `pnpm build` 與 `pnpm audit --prod --audit-level high`。新指令為原指令的嚴格超集合，驗收語意未放寬，無 FR/SC 新增或移除。 |
| 2026-08-24 | Issue #294（ADR-033 治理條文改寫）：狀態機簡化，移除 `plan-ready`／`tasks-ready` 兩態，新增 `change-open`（對應 `openspec/changes/<change>/` propose 已建立、尚未 archive）；`foundation-000` 與 `account-001` 由 `plan-ready` 改標 `spec-ready`，其 `plan.md` 分別保留為常設架構文件（ADR-033 例外）與首個 OpenSpec change 的 `design.md` 參考素材。純狀態機與備註欄調整，不影響 spec 版本號。 |
| 2026-08-24 | Issue #261 drift 修正（最後一項，issue #261 涵蓋範圍全數完成）：`dashboard-012` 更新至 v2.4.1（FR-018／FR-019、SC-017／SC-018）——`task_membership` 先前完全同步渲染、無 Skeleton 亦無 API 錯誤／重試狀態。新增模擬非同步載入（`MEMBERSHIP_LOAD_DELAY_MS = 400`，原型無後端）：載入中顯示 Skeleton（指標卡＋任務列表佔位塊、`aria-busy="true"`）；失敗時結束 Skeleton、顯示 i18n 錯誤訊息與重試按鈕，不 fallback 一般使用者視圖、不清除 session、不導向 `/login`；重試比照 `task-list.html` 既有慣例視為必定成功。沿用既有 `?view=error` 慣例並新增 `?view=skeleton`（僅原型測試 hook，非正式產品 query）供決定性測試。新增 `dashboard-membership-load.spec.ts`（5 案例）；連帶修正既有 overflow 測試誤以恆常可見的 `dashboard-shell` 作為就緒訊號、實際量測 Skeleton 版面而非最終內容版面的問題。`tests/dashboard/`（65）、`tests/shared/`、`tests/cross-role/` 相關測試全數通過。規格條文本已正確，僅原型未落實。issue #261 的文件層（15 UI spec traceability）、proto-sync.md 更新、6 項行為 drift 至此全數完成。 |
| 2026-08-24 | Issue #261 drift 修正：`dataset-017` 更新至 v2.1.2（FR-002／`INVALID_TASK_TRIGGER`）——`dataset-analysis-detail.html` 的 `applyRouteTask()` 先前對未知或缺漏 `task_id` 靜默回退至 `DEFAULT_TASK_ID`（T001），與規格「導回 `/dataset-analysis` 並顯示錯誤提示」不符（規格條文本已正確，僅原型未落實）。修正為 init 階段驗證 `task_id` 存在於 `TASK_META`，不存在則導向列表頁並帶 `invalid_task=1`，列表頁顯示錯誤 toast（新增 i18n 鍵 `toastInvalidTask`）。TDD：先將既有鎖定回退行為的測試（`dataset-analysis-detail-registry-mirror.spec.ts`）改為斷言正確導回行為並新增缺漏 `task_id` 情境，確認紅燈後才實作；連帶修正兩個原依賴回退行為省略 `task_id` 的既有測試。`tests/dataset/`（76）、`tests/shared/`＋`tests/cross-role/`（109，2 項 pre-existing D2 gap `test.fail()` 不計入）、`tests/dashboard/`（60）全數通過，`tsc --noEmit` 乾淨。 |
| 2026-08-24 | Issue #261 drift 修正：`admin-007` 更新至 v1.1.13——核實 git 歷程與現行 `role-settings.html`／`role-settings.spec.ts`，發現 v1.1.7 記錄的「移除頂部未儲存變更 banner」與實際程式碼不符：banner 於 PR #50 review 後（`524dfbd`）刻意加回並持續維護，有 6 個既有 Playwright 斷言鎖定其顯示/隱藏、具 `role="alert"` 可及性語意；取消確認對話框（區塊 E）也已正確實作，並非直接放棄變更。banner 與確認對話框為互補關係，新增區塊 G 補齊介面定義；純文件修正，v1.1.7 歷史記錄保留不變，不改變任何 prototype 程式碼或測試。 |
| 2026-08-24 | Issue #261 drift 修正：`dataset-016` 更新至 v2.1.2——更正「Prototype 示例資料基線」IAA 示意分布敘述，`pass = 6`／`not_started = 2` 為 v2.1.0 引入 `not_applicable` 徽章狀態前的舊值、未同步更新；實際 prototype fixture 與既有回歸測試（`IAA_FILTER_COUNTS`）皆為 `pass = 5`、`not_started = 1`、`not_applicable = 2`（T009／T012 兩筆 `free_text`-only 任務，依 FR-004A 顯示 `not_applicable`）；純文字修正，不改變任何 prototype 程式碼或測試。 |
| 2026-08-24 | Issue #261（Prototype Traceability，文件層，第三批）：`shared-008` 更新至 v1.4.2——新增 `## Prototype Traceability` 表格，明確對應共用 `sidebar.js`／`sidebar.css`（14 個跨模組消費頁面：account 5、admin 2、annotation 2、dashboard 1、dataset 2、task-management 3）與 living styleguide（`components-showcase.html`）的責任邊界；規格條文未變，純 patch 版本。issue #261 的 15 份 UI spec 文件層工作至此全數完成，僅剩 proto-sync.md 反向映射規則更新與 7 項 drift 修正拆入後續 PR。 |
| 2026-08-24 | Issue #261（Prototype Traceability，文件層，第二批）：`dataset-016` 更新至 v2.1.1、`dataset-017` 更新至 v2.1.1——新增統一格式的 `## Prototype Traceability` 表格，明確對應 dataset-analysis-list 原型頁面、dataset-analysis-detail 主頁 shell 與 Stats／Quality 兩組共 16 個逐輸出類型 partial 的責任邊界；所有相對連結皆已驗證實際存在。規格條文皆未變，純 patch 版本。第一批（task-management 010/013/014、annotation-015）另案 PR；剩餘 shared-008 與 proto-sync.md 更新拆入後續 PR；issue 本文列出的 dataset-017 無效 task ID drift（第 1 項）不在本次範圍內。 |
| 2026-08-24 | Issue #261（Prototype Traceability，文件層，第一批）：`task-management-010` 更新至 v2.1.1、`task-management-013` 更新至 v6.9.4、`task-management-014` 更新至 v2.10.4、`annotation-015` 更新至 v4.20.1——四份 spec 新增統一格式的 `## Prototype Traceability` 表格（Artifact／Responsibility／Covered FR-SC／Verification／Status），明確對應各自的原型主頁、partial／共用引擎、頁面資料層與設計層參考的責任邊界；013／014 標註共用 `OUTPUT_TYPE_REGISTRY` 設定引擎的共同擁有關係，014／015 標註共用 `modal-focus.js` 的共同擁有關係。所有相對連結皆已驗證實際存在（`scripts/check-spec-artifacts.sh` 通過）。規格條文皆未變，純 patch 版本；issue #261 本身涵蓋 15 份 UI spec，本次僅完成 task-management／annotation 四份，dataset／admin／shared-008 與 proto-sync 更新拆入後續 PR（account 五份、dashboard-012、admin 006/007 已於先前 PR 完成，issue checklist 尚未同步勾選）；issue 本文列出的 7 項 drift（dataset-017 無效 task ID 回退、admin-007 dirty banner、dashboard-012 skeleton/error-retry 等）不在本次範圍內。 |
| 2026-08-24 | `annotation-015` 更新至 v4.20.0（`feat/annotation-workspace-evidence-fields`，issue #89）：task-management-013 v3.1.0（FR-003g-2）已將 Evidence 自 Step 2 標記預覽移除並明定「其內容留待標記工作區呈現」，但工作區先前僅由 FR-024H／AC-2B.3 涵蓋 `free_text`（唯一宣告 `rendersEvidencePreview: true` 的輸出類型），其餘 7 種 `OUTPUT_TYPE_KEYS` 即使 `field_role_map` 已指定 Evidence 角色欄位也從未呈現，013 的延後承諾沒有落點。新增 FR-024N／AC-2B.5：`field_role_map` 存在 evidence 角色欄位且已選輸出類型皆未自行呈現 Evidence 時，工作區於題目卡最上方顯示唯讀「背景參考 (Evidence)」卡片（純文字、無可編輯控制項），位置早於 Input 內容卡與所有作答控制項。原型新增 `renderEvidenceReferenceCard()`（annotator-only，重用既有 `ws-evidence-card` testid，跳過引擎已渲染的情況以避免重複），不修改 `task-config.engine.js` 共用引擎的 `rendersEvidencePreview` 閘門（task-new Step 2 預覽行為不變）。新增 2 個 Playwright 測試（T005 `multi_dim` 正向路徑、T001 `single_label` 負向路徑）。 |
| 2026-08-24 | `annotation-015` 更新至 v4.19.0（`feat/reviewer-row-decision-draft-persistence`，issue #196）：審核員逐列 `通過`/`退回` 決策草稿持久化——`w6-resilience-a11y.md` CONT-03 核實出送出審核前的逐列決策僅存於記憶體，reload 即遺失，與標記員草稿持久化不對稱；issue 本文建議「提供以維持角色對稱」，因無同步產品負責人可裁決，依 issue 自身建議定案。新增獨立於提交 bucket 的草稿儲存區（`labelsuite.wsReviewDecisionDrafts.<bucketKey>`，刻意不重用 `markSampleSaved` 以避免歷程洗版與 FR-062 盲審隔離違反），決策每次變更即寫入、送出後清除。新增 FR-014S、AC-6.10；不涵蓋直接修正控件文字/數值編輯持久化、仲裁投票（issue #319，另案）或說明彈窗（issue #195／#287，另案）。 |
| 2026-08-24 | `annotation-015` 更新至 v4.18.1（`fix/ws-arbitration-store-lost-update`，issue #319，issue #283 code review 發現之同型未修範圍）：仲裁儲存層 lost-update 修復——`labelsuite.wsArbitration` 由單一整包 key 改為每爭議項獨立 key（`<bucketKey>::<sampleId>::<itemId>`）；此 store 的 bucket key 刻意不含 `reviewerId`（多位仲裁者共用 bucket）且一個 bucket 橫跨該標記員的所有樣本／爭議項，故僅拆至 #283 的 bucket 粒度不足以修復，需拆到爭議項粒度；開機一次性遷移舊 blob、corrupt 值降級無此項、item 列舉固定排序；規格條文未變（同一爭議項維持原型層 last-write-wins）。新增真並行雙仲裁者不同爭議項送出（Promise.all）／遷移／corrupt 降級回歸測試。 |
| 2026-08-24 | `task-management-013` 更新至 v6.9.3（`feat/task-new-wizard-draft-warning`，issue #197）：issue 回報 task-new 精靈重新整理／誤觸上一頁即遺失全部進度、無離頁提示，經查該保護已由既有 FR-007a（未儲存變更離頁前確認視窗）與 v3.3.1（2026-07-09，session storage 精靈狀態持久化）落地，w6-resilience-a11y 稽核快照落後於實作、issue 建立時已非回歸。本次僅新增 Prototype Playwright 回歸測試補齊此前缺口（dirty 時 `beforeunload` 觸發／clean 時不觸發／reload 後草稿還原），規格條文未變；同時記錄 `localStorage` 草稿持久化評估結論——維持現行 `sessionStorage`（分頁天然隔離、避免共用電腦草稿殘留），不採用。 |
| 2026-08-24 | `task-management-014` 更新至 v2.10.3（`fix/guideline-attachments-per-task`，issue #185）：`DEFAULT_GUIDELINE_FILES`（task-detail.data.js）修正——原對 17 個 seed profile 全數附加同一張 VA 情緒量表圖（`VA_emj.png`）＋一份指向不存在目錄的死連結 PDF；圖片項目改為明確標示「通用範例圖」的占位 SVG（新增 `assets/images/task-management/generic-guideline-example.svg`），維持所有任務共用同一份 config-driven 檔案清單（不逐任務類型硬編，符合 Generalization-First）；PDF 項目直接移除（不虛構假檔案）。annotation-workspace 消費端（annotation-015）同步驗證正常渲染，規格條文未變。新增回歸測試：逐 profile guideline file 存在性 assertion（200 回應）＋非 VA 任務範例圖標示斷言。issue-180 finding register F-02 標記已修復。 |
| 2026-08-24 | `task-management-013` 更新至 v6.9.2（`fix/task-new-submit-persistence`，issue #285）：prototype `task-new.html` 的 `submitTask()` 先前只產生 `task_id` 並導頁、未寫入任何資料層——新任務清單看不到、task-detail 落 not-found。修正為將精靈已收集欄位寫入 `labelsuite.createdTasks` localStorage bucket，`task-list.data.js`／`task-detail.data.js` 載入時合併進 `LabelSuiteTaskListData.tasks`／`LabelSuiteTaskDetailData.profiles`，建立任務以 `draft` 狀態出現在任務清單並可從 task-detail 開啟；規格條文未變（僅補齊 FR-006a／SC-002 描述在 prototype 端過去未落地的示範缺口）。issue #212 xrole 正典旅程 XROLE-01/02 斷言同步改誠實成功狀態（wizard-created 任務不再斷言 not-found）。 |
| 2026-08-24 | `annotation-015` 更新至 v4.18.0（`fix/shared-modal-focus-management`，issue #195，F-11，WCAG 2.1 AA 2.4.3）：`#wsGuidelineModal`／`#wsGuidelineImageModal` 補上鍵盤焦點管理——新增共用模組 `design/prototype/pages/shared/modal-focus.js`（`window.LabelSuiteModalFocus`，比照 `sidebar.js` 共用元件模式），開啟時焦點移入第一個可互動元素、`Tab`／`Shift+Tab` 侷限於 modal 內、`Esc` 關閉、關閉後焦點歸還觸發元素（`#wsGuidelineModal` 無觸發元素，歸還至新增 `tabindex="-1"` 的 `#wsRoot`）；規格條文未變。新增 Playwright 回歸測試落地 `w6-resilience-a11y.md` A11Y-01 情境。**部分完成**：A11Y-02（task-management `#riskModal`／`#deleteTaskModal`）延後至後續 PR（單一 PR 5 檔／300 行上限），維持「待缺陷修正後啟用」。 |
| 2026-08-24 | `annotation-015` 更新至 v4.17.0（`feat/annotation-list-text-truncation`，issue #188，源自 issue #180 finding F-05）：`annotation-list` 長文本任務（QA／摘要／多輪對話種子 T009／T012／T013）清單列因 `.text-snippet` 停用截斷而整段原文換行、列高過高降低可掃描性——改為預設固定行數 CSS line-clamp，超出截斷者提供「展開全文」toggle（`list-text-toggle`），toggle 點擊 `stopPropagation` 不與既有列點擊導頁至 `annotation-workspace` 行為衝突（Annotator／Reviewer 視圖皆適用）。新增 FR-007I、AC-1.19；原型兩處手動組裝文本儲存格改為共用 `buildTextCell()` helper，新增 5 個 Playwright 回歸測試。 |
| 2026-08-24 | `dashboard-012` 更新至 v2.4.0（`feat/dashboard-task-list-sorting`，issue #187）：Annotator／Reviewer 任務列表新增排序下拉控制（預設順序／進度：高到低／進度：低到高），複用既有 `progress` 數值欄位；不影響既有卡片欄位、CTA 與導頁行為，語言切換即時更新控制文案（新增 FR-010D、FR-011F、SC-026）。範圍說明：`dashboard.assignments.js` 未提供「最後提交時間」或「待審筆數」獨立數值欄位，故不新增此二排序鍵；該資料模型亦僅為每任務單一 annotator／reviewer work item（非逐使用者指派），故「清單呈現使用者實際被指派子集」維持現況全量顯示 17 筆 prototype 基線任務，未新增個人化篩選。issue-180 finding register F-04。 |
| 2026-08-24 | `task-management-014` 更新至 v2.10.2（`docs/spec-014-small-sample-iaa-advisory`，issue #207）：Overview「抽樣設定」唯讀摘要區塊（FR-010o）新增小樣本 IAA 忠告文案——013／014／015／017 全文僅有 `sampling_value >= 1` 的表單合法值下限，未定義統計有效性建議門檻，PL 設定極小抽樣（如 n=2）時 IAA 指標方差過大、不具統計推論意義卻無提示。新增 FR-010o-2：`sampling_value < IAA_SMALL_SAMPLE_THRESHOLD`（沿用 `dataset-017` 規格常數、現行值 5）時，緊鄰 `sampling_value` 顯示雙語忠告文案（zh／en 兩語系皆定義），唯讀提示、不阻擋操作，不改變既有驗證下限或任何 IAA 計算邏輯。純措辭層級新增、非新功能；prototype 端呈現為後續獨立實作 PR（spec-first）。 |
| 2026-08-24 | `annotation-015` 更新至 v4.16.0（`docs/spec-015-guideline-gate-fr`，issue #287）：指引閘門 modal 之正式契約補完——task-management-013 FR-005c／SC-006a 已定義觸發行為，但 015 從未定義 workspace 端資料模型。新增 FR-066：（1）觸發判定沿用 `TaskProfile.forceShowGuideline`（即 013 `force_guideline`）；（2）modal 內容與右欄說明面板同源（`guidelineFiles` markdown 條目），不另維護第二份文字；（3）確認紀錄逐 `task_id` 記錄，原型以 `localStorage`（`labelsuite.guidelineModalSeen.<task_id>`）示範，正式儲存實體留待後端定義；（4）新增契約——指引內容異動後既有確認紀錄須失效並強制重新確認，`TaskProfile` 須攜帶指引版本標記（具體形狀留待後端定義）。同步擴充 `TaskProfile`／`GuidelineAsset` 實體。純 spec 補完，第 1–3 點為既有原型行為之正式化、第 4 點為新增契約，原型化屬後續 PR 範圍。 |
| 2026-08-24 | `annotation-015` 更新至 v4.15.0（`docs/spec-015-arbitration-vote-idempotency`，issue #288）：仲裁重複投票（改票）之冪等語意補完——`submitArbitration()`（`annotation-workspace.data.js:1751-1787`）自 issue #199 起已將同一仲裁者對同一爭議項的重複送出改為覆寫 `votes[]` 既有條目，但此前僅為實作選擇、spec 從未定義為正式契約。新增 FR-065：覆寫、不新增（idempotent PUT、不得 409）；改票僅限審核單位仍 `disputed` 期間，`finalized` 後受既有 FR-053（v4.14.0）唯讀鎖定約束、不另發明解鎖流程；覆寫後 `voted_at` 更新但不保留覆寫前歷史值；`ReviewUnit.status` 與收斂結果皆為推導、不快取，改票後無需另行觸發重算步驟。純 spec 補完，不改變任何原型行為。 |
| 2026-08-23 | `task-management-014` 更新至 v2.10.1（`fix/task-detail-results-fallback`，issue #284）：annotation-results 面板與匯出修正——未知 `task_id` 不再靜默回退 T001 seed，改空集合（面板 `arEmptyState` 空狀態、匯出零筆），T009／T012 明確登錄；規格條文未變。issue-180 finding register N-05 標記已修復；xrole 正典旅程 XROLE-19/22 斷言同步改誠實空狀態。 |
| 2026-08-23 | `annotation-015` 更新至 v4.14.1（`fix/ws-submission-store-lost-update`，issue #283）：提交儲存層 lost-update 修復——`labelsuite.wsSubmissions` 由單一整包 key 改為每 bucket 獨立 key（並行寫入構造上不重疊），開機一次性遷移舊 blob（保留訪客草稿與 demo seed）、corrupt 值降級空 bucket、bucket 列舉固定排序；規格條文未變（同 bucket 維持原型層 last-write-wins）。新增真並行儲存（Promise.all）／遷移／corrupt 降級回歸測試；issue-180 finding register N-01 標記已修復。 |
| 2026-08-21 | `dashboard-012` 更新至 v2.3.1（`fix/dashboard-card-a11y-task-type`，PR #315，issue #311）、`annotation-015` 更新至 v4.13.0／v4.14.0（`fix/empty-review-unit-gate`，PR #316，issue #307；`fix/finalized-unit-lock`，PR #317，issue #308，stacked 於 #316）：雙軌驗收餘下三單全數修復——①空審核單位（無儲存提交且無 mock 遞補列）改空狀態卡並封鎖送出，標記員實際提交後即恢復可審（AC-3.38）；②`FINALIZED` 單位完全唯讀鎖定，門檻收斂與仲裁送出兩條定稿路徑皆鎖、FR-016A 帶理由重開延後後端（AC-3.39；連帶修正 3 個依賴缺陷行為方能通過的既有測試）；③dashboard 任務卡列補鍵盤可達性並恢復 `task_type` 參數（eff1938 歷史回歸）。issue #307／#308／#311 全數關閉，T014–T017 雙軌驗收發現至此清零（P1 呈現層缺口另行追蹤）。 |
| 2026-08-21 | `dashboard-012` 更新至 v2.3.0（`fix/demo-review-progress-semantics`，PR #312，issue #310）、`annotation-015` 更新至 v4.12.1（`fix/reviewer-workspace-vocab`，PR #313，issue #309）：T014–T017 merge 後雙軌實機驗收（Claude 4 sub agent 實操 + Codex 唯讀走查）揪出的兩項 [High] 呈現層缺陷修正——dashboard 示範列百分比統一改「審核覆蓋率」語意並於 T016 揭示「未定稿 3 筆 · 爭議 1 筆」（新增 FR-011E）；workspace reviewer 視角三處字串依 role 分流（左欄五態語彙／「已審 x / n」／側欄「審核員」，annotator 視角不變）。其餘驗收發現追蹤於 issue #307／#308／#311。 |
| 2026-08-21 | `task-management-010` 更新至 v2.1.0、`dashboard-012` 更新至 v2.1.0／v2.2.0、`annotation-015` 更新至 v4.11.0／v4.12.0（`feat/review-flow-demo-tasks`，PR #305，issue #302 審核流程示範任務）：新增 T014–T017 四組 `single_label` 審核流程示範任務（試標共識／單一終審員／三人多數決／雙人平手仲裁，各 5 筆樣本、8 個 config + example-data 檔入 `docs/product/`），boot-time 冪等 seeder（`labelsuite.reviewFlowDemoSeed.v1`）播種 29 個審核單位覆蓋五態機、FR-061 多數決收斂／全數分歧／平手與仲裁入口；工作區新增 FR-064 審核單位脈絡橫幅、dashboard 新增 FR-011D 示範列 reviewer 身分導覽使仲裁初始畫面自儀表板可達；既有 13 筆任務資料不動，016 Dataset Analysis List 刻意不隨擴充。 |
| 2026-08-21 | `dashboard-012` 更新至 v2.0.5（issue #186）：Super Admin／Project Leader「等待 IAA 確認」指標卡從純文字改為可點擊、鍵盤可操作的待辦入口，導向 `/task-list?status=waiting_iaa_confirmation`；prototype 示範資料同步調整，使指標卡數字與 `/task-list` 篩選後筆數對帳（新增 FR-008F、FR-009A1、SC-025）。 |
| 2026-08-19 | `dashboard-012` 更新至 v2.0.3、`shared-008` 更新至 v1.4.1（`fix/191-reviewer-copy`，PR #258）：issue #191——Dashboard Reviewer 行動卡與快捷鍵總覽文案仍描述已廢止的二元「通過/退回」批次審核模型。dashboard-012 v2.0.3：行動卡 C 內文改「逐標記員獨立審核，直接修正結果，歧異交付仲裁。」、步驟 2 改名「修正或仲裁」（附註「直接修正結果；退回僅限正式標記」），對齊 annotation-015 v4.x 逐標記員審核單位模型；shared-008 v1.4.1：快捷鍵總覽 `R` 列標籤補上適用範圍註記「退回目前結果（限正式標記）」（annotation-015 AC-3.15／AC-6.4 將退回通道收斂為 official_run 專屬），`A` 列不變。 |
| 2026-08-18 | `task-management-014` 更新至 v2.6.0（`feat/issue-688-align-014-review-model`）：審核員模型重構 P5 之二（issue #149）。教授三項要求裡工時紀錄缺的是「實際審核了多少筆」——`work-log` 工時明細表 `完成筆數` 拆為 `標記筆數`／`審核筆數`／`仲裁筆數` 三欄，角色不適用的欄位顯示 `—`（標記員僅標記筆數；審核員僅審核／仲裁筆數）；匯總卡片改為 `總工時`、`總標記筆數`、`總審核筆數`、`加權平均速度` 四張，`加權平均速度` 卡附「每筆平均耗時」次要說明列；逐列平均速度與異常提醒計算改以三類筆數總和為分子；`WorkLogEntry.completed_count` 拆為 `annotated_count`／`reviewed_count`／`arbitrated_count`。新增 FR-007b、SC-036；原型 seed 拆欄＋4 個 Playwright 測試。 |
| 2026-08-18 | `task-management-014` 更新至 v2.5.0（`feat/task-management/014-review-history`）：審核員模型重構 P5 之一（issue #149）。教授回饋要求逐筆看見「誰標、誰審、誰仲裁」——`annotation-results` 展開列於每位標記員子列下新增縮排審核歷程時間軸（審核員名稱＋`同意`／`修改→{修正後結果}`＋審核時間；經仲裁再一行仲裁者＋`採 A`（維持標記員）／`採 B`（採用審核員修正）＋時間；`待審` 不顯示歷程行、逐標記員各自成段不合併）；審核狀態 badge 語彙自 `通過／退回／待審核` 三態改為 `AR_REVIEW_STATUS` 五態（待審／已同意／已修改／爭議中／已定稿，沿用 015 `REVIEW_UNIT_STATUS`）；篩選列新增「審核員」（reviews[] + 仲裁者去重推導）與「審核狀態」（常數推導）下拉，與既有篩選 AND 組合。新增 FR-015a-1／FR-015d-4／SC-034／常數 `AR_REVIEW_STATUS`。原型：seed 全面補具名 `reviews[]`／`arbitration`、9 個 Playwright 測試。工時紀錄完成筆數拆欄（總審核筆數卡、每筆平均耗時）屬 P5 後續 PR。 |
| 2026-08-18 | `task-management-014` 更新至 v2.4.0（`feat/task-management/014-review-assignment`）：審核員模型重構 P4 之二（issue #148）。v2.3.0 讓 `project_leader` 設定得了 `min_reviewers`／指派方式／仲裁者，但 manual 模式在成員管理端沒有任何落地介面——審核量分布、未指派池與爭議池對 PL 完全不可見。新增 FR-005i ~ FR-005k／SC-034／使用者故事 1 驗收情境 6：成員清單「任務角色」與「狀態」間新增「審核負荷」欄（annotator `—`；reviewer `{assigned} 筆 · {pending} 待審`，assigned 恆為 pending + done 推導值）；成員清單後新增「審核指派」區塊（未指派筆數、每位 active reviewer 的已指派／待審／已完成三欄、生效仲裁者「仲裁」標籤）；auto 模式整區唯讀僅顯示輪派結果，manual + PL 提供「自動補齊」（輪流分配至未指派 0）與逐列「指派…」；底部爭議池列 + 「分派給仲裁者」（輪流分派給生效仲裁者；無仲裁／無生效仲裁者／爭議池 0 時停用）。新增 `ReviewAssignment` 實體（reviewer ↔ 審核單位連結，source = auto_rotation／manual／dispute_dispatch），`TaskMembership` 註記審核負荷為推導值、仲裁非新任務角色。原型：member-management partial 新增欄與區塊、i18n 雙語 20 鍵、成員 seed 擴充兩位 reviewer、7 個 Playwright 測試（全套 733 綠）。 |
| 2026-08-18 | `task-management-014` 更新至 v2.3.0（`feat/task-management/014-review-settings`）：審核員模型重構 P4 之一（issue #148）。P1 ~ P3 已讓提交定址、審核粒度與爭議池全面以 `min_reviewers` 位審核員為前提，但 014 的 Overview 從無任何入口設定這個 N——`review_assignment_mode`／`agreement_auto_finalize`／`arbitration_enabled`／`arbiter_ids` 同樣只存在於 015 的消費端假設中。新增 FR-010s ~ FR-010s-2／SC-033／使用者故事 3 驗收情境 7–8：「審核設定」區塊落在抽樣設定之後、編輯權限同規則（`draft` + `project_leader`）；`min_reviewers` 為可直接鍵入的整數框（>= 1，`1` = 單一終審員）；仲裁者候選 = active reviewer 成員、可留空 = 任一未參與者可認領（對齊 015 FR-060），仲裁停用時選單整個隱藏；仲裁摘要值三態（停用／啟用 · 未指定仲裁者／啟用 · 仲裁者 N 人）。原型：overview panel 新增區塊 + 編輯狀態機（鏡射抽樣設定模式）、i18n 雙語 29 鍵、7 個 Playwright 測試。成員管理「審核負荷」欄、審核指派區塊與 `ReviewAssignment` 實體屬 P4 後續 PR。 |
| 2026-08-18 | `annotation-015` 更新至 v4.8.0（`feat/annotation/015-dispute-arbitration`）：審核員模型重構 P3c（issue #147，P3 收尾）。v4.7.0 的 `仲裁` 入口導向工作區後仍渲染 FR-053 審核卡——仲裁者只能重新當一次審核員，`DisputeItem` 的 `votes[]`／`finalized_*` 欄位定義了卻沒有任何寫入路徑。新增 FR-061／AC-4.22 ~ AC-4.24／SC-004T／常數 `DISPUTE_CONVERGENCE_RULE`，並修訂 FR-051（`disputed` 不再是終態）。三項設計決策：仲裁者選邊不重標（修正控件與 ✕/✓ 於仲裁版面 0 節點）；多數決收斂為推導而非寫入（嚴格多數 > N/2 含對標記員值的隱含同意票，N=1／偶數平手／全數分歧進池）；仲裁狀態以審核單位定址儲存、不入 reviewer bucket（任何仲裁者的定案對所有檢視者可見）。原型：`getArbitrationState()`／`submitArbitration()`／`resolveDisputeConvergence()` + `getReviewUnitStatus()` 延伸 + 仲裁版面渲染分支，新增 14 個工作區仲裁測試。 |
| 2026-08-18 | `annotation-015` 更新至 v4.7.0（`feat/annotation/015-dispute-list-entry`）：審核員模型重構 P3b（issue #147）。五態篩選自 v4.2.0 起即提供 `爭議中` 選項，但爭議列與一般列在動作上毫無差別——具仲裁資格的審核員無從辨識哪些爭議可由自己認領，v4.6.0 定義的 `DisputeItem` 也沒有任何介面抵達路徑。新增 FR-060／AC-1.18／SC-004S：`disputed` 列對合格仲裁者將 `編輯` 換為 `仲裁`（`list-arbitrate-entry`）；資格＝名冊 `can_arbitrate` 旗標 AND 非當事人（該審核單位查無自己的 reviewer 提交，FR-049 定址）；`仲裁` 與 `編輯` 導向同一網址並攜帶完整審核單位身分、不新增參數——工作區依身分與單位狀態自行決定版面（P3c），入口不預先綁定版面語意；資格判定函式刻意不含 `disputed` 狀態檢查（清單列本就為顯示推導過單位狀態，塞進函式等於強迫每個呼叫端付出第二次全量推導）。原型：`isArbiterCandidate()` + 爭議列 CTA 分支 + i18n `arbitrateBtnLabel`，新增 5 個清單測試含 `爭議中` 篩選正向路徑（既有測試僅涵蓋空清單負向路徑）。 |
| 2026-08-18 | `annotation-015` 更新至 v4.6.0（`feat/annotation/015-dispute-item-model`）：審核員模型重構 P3 之一（issue #147）。FR-051 宣告 `disputed` 為終態且「為後續爭議池仲裁的輸入」，但從未定義一個爭議是什麼——爭議池、逐項 A/B 仲裁與多數決收斂無從掛載。新增 FR-059／AC-4.19 ~ AC-4.21／SC-004R／關鍵實體 `DisputeItem`／常數 `DISPUTE_ITEM_SOURCE`：爭議項於每次讀取時由 FR-052 差異比對推導、不實體化儲存（與 `ReviewUnit.status` 同一哲學，爭議池在結構上不可能與狀態機漂移）；識別為 `outKey × 合併鍵` 且跨審核員合併，A/B 值取差異兩側、與標記員一致的審核員不得出現，拆解粒度沿用 FR-052（實體改型拆兩項、`multi_dim` 逐維度）；仲裁投票 `votes[]` 與定案值 `finalized_*` 為僅有的寫入狀態，欄位先定義、寫入行為留待後續 P3 PR。原型：`getDisputeItems()` + `readReviewerSubmissions()` 改攜帶 `reviewerId`，9 個資料契約測試釘住推導行為。仲裁介面與清單入口屬 issue #147 後續拆分。 |
| 2026-08-18 | `annotation-015` 更新至 v4.5.0（`fix/annotation/015-workspace-shortcuts`）：issue #152。共用側欄快捷鍵總覽於「工作區」分類列出 `Ctrl/Cmd + S`／`Ctrl/Cmd + Enter`／`Alt + ←`／`Alt + →`，四者全數從未實作，總覽持續教使用者按下沒有效果的鍵（v4.1.0 已就 `A`／`R` 修正同類問題，本版處理其餘四個）。新增 FR-058／AC-4.18／SC-004Q：快捷鍵一律轉派對應按鈕的 click 而非直接呼叫處理器——按鈕已承載角色配置（reviewer 的 `儲存草稿` 隱藏且未綁定）與首/末審核單位的 `disabled`，重新推導等於複製一份會漂移的條件；`Ctrl/Cmd + Enter` 依角色分流至 `提交`／`送出審核` 並各自經既有完整性驗證；與 FR-054 刻意相反，此四者在輸入焦點時照常生效（修飾鍵組合不產生文字，且 `儲存草稿` 最需要的時機正是 `free_text` 作答途中）；`preventDefault` 為必要行為（`Ctrl/Cmd + S` 開啟瀏覽器儲存對話框、`Alt + ←` 為上一頁）。連帶移除 i18n 死字串 `reviewBulkApproveLabel`／`reviewBulkRejectLabel`（中英各一對，自 v4.1.0 廢止批次快捷鍵後零引用）。 |
| 2026-08-17 | `annotation-015` 更新至 v4.4.0（`fix/annotation/015-workspace-url-sync`）：issue #151。`annotation-workspace` 開頁時讀取 `sample_id`，但切換樣本後從不寫回網址列——網址永遠停在進入時的那一筆，重新整理會跳回進入樣本、複製連結分享會開到別筆、瀏覽器上一頁／下一頁對樣本切換完全無效。v4.3.0 之後網址定址的是一個審核單位，只同步 `sample_id` 只修一半（reviewer 重整後仍落到預設標記員）。新增 FR-057／AC-4.16 ~ AC-4.17／SC-004P：切換時以原地取代（`replaceState` 語意）寫回 `sample_id`（所有角色）與 `annotator_id`（僅 reviewer）。三項刻意取捨：採取代而非堆疊（否則走訪 15 個審核單位需按 15 次上一頁才離得開）、就地更新查詢字串而非以已知鍵重建（避免靜默丟參數）、annotator 視角不寫入 `annotator_id`（其身分翻筆時不變，寫入等於憑空新增進入連結沒有的參數）。開頁 `sample_id` 查無對應樣本的回退亦一併正規化，使重新整理具冪等性。 |
| 2026-08-17 | `annotation-015` 更新至 v4.3.0（`feat/annotation/015-workspace-review-unit-nav`）：審核員模型重構 P2 之五（issue #146）——v4.2.0 已把 `annotation-list` 攤平為一列一個審核單位，工作區卻仍是樣本形狀：左欄逐 `datasetRecords` 渲染而無角色分支、進度分母取 `datasetRecords.length`、`annotator_id` 開頁解析後即固定。審核員自清單點進「`sent-001` × 第一位標記員」後，`下一筆` 直接跳到 `sent-002` 且身分不變，同一樣本另外兩位標記員在工作區內沒有任何抵達路徑，v4.1.0 的「`A` 通過 → `下一筆`」快速通道每輪只能覆蓋三分之一的審核單位。新增 FR-056／AC-4.12 ~ AC-4.15／SC-004O：左欄一列一個審核單位（標示 `樣本 ID · 標記員帳號`，名冊沿用 `annotation-list` 同一個 `getReviewerMockRows()` 故兩頁筆數不漂移）、進度分母計審核單位數且逐單位查身分 bucket、翻頁每次前進一個審核單位並同步 `annotator_id`、作答快照改以 `sample_id + annotator_id` 為鍵（原鍵僅 `sample_id`，會讓審核員對前一位標記員的未送出修正顯示在下一位名下）。連帶修訂 FR-013B 與 US2 區塊 B 左欄描述；`dashboard-output-types` 測試中「左欄項目數 = `datasetRecords.length`」的斷言改為依角色推導。annotator 視角不變。 |
| 2026-08-17 | `annotation-015` 更新至 v4.2.0（`feat/annotation/015-list-review-granularity`）：審核員模型重構 P2 之四（issue #146）——v4.0.0 已讓工作區「一張卡審一位標記員」，`annotation-list` 卻仍停在「一列一筆樣本」：`dry_run` 需展開才看得到標記員、`official_run` 更把三位標記員截斷成一位，審核員無法在清單列出、篩選或定位到實際的審核標的；清單層級的通過/退回還與審核卡構成兩個互相矛盾的決策來源。新增 FR-055／AC-1.14 ~ AC-1.17／SC-004N：清單粒度＝`REVIEW_UNIT_DIMENSIONS`（同一樣本 N 位標記員 → N 列，兩種 `run_type` 一致），每列顯示該標記員本人答案與該單位的 `REVIEW_UNIT_STATUS`；狀態篩選依角色由常數推導（reviewer 五態、annotator 維持三態）；導頁必須帶出該列 `annotator_id`（沿用 FR-049）；標記分布統計欄存續為唯讀跨標記員脈絡。廢止 FR-047／FR-048／FR-027／AC-1.10 ~ AC-1.13、展開控制項與逐列決策控件、`送出審核` 按鈕與其 toast、逐列一致度／IAA 摘要，testid 與 i18n key 一律保留不重用；AC-1.3 同步修訂（reviewer 列點擊改為導頁）。 |
| 2026-08-17 | `annotation-015` 更新至 v4.1.0、`shared-008` 更新至 v1.4.0（`feat/annotation/015-review-shortcuts`）：審核員模型重構 P2 之三（issue #146）——共用側欄的快捷鍵總覽自 spec 008 起就列出四個審核快捷鍵，但工作區從未實作其中任何一個。annotation-015 新增 FR-054／AC-3.37／SC-004M 定義 `A`＝通過、`R`＝退回，作用對象為當前審核單位的**全部**輸出類型（審核單位為「樣本 × 標記員」，介面無「目前聚焦輸出類型」概念，且 FR-044 要求每個 outKey 皆有決策方可送出），重複按鍵取消回未決策，輸入焦點（`input`／`textarea`／`select`／contenteditable）與修飾鍵不觸發，`role = annotator` 不註冊；批次快捷鍵 `Shift+A`／`Shift+R` 隨審核單位收斂為單一標記員而廢止，其總覽列移除由 shared-008 v1.4.0 的 FR-016G／SC-009D 承接。 |
| 2026-08-17 | `annotation-015` 更新至 v4.0.1（`fix/annotation/015-reviewer-seed-source`）：issue #161。審核面板的初始值被資料集 output 角色欄位覆寫——`buildAnnotatorRecord()` 於 sanitize 後回填 output 角色欄位（013 FR-003g-5 對標記員的預填設計），reviewer 沿用同一份紀錄，使共用引擎的預設合併路徑一律以 gold 勝出：T001/sent-001 標記員 `113450022` 提交 `negative`，面板卻顯示 `gold_label` 的 `positive`。修法為將回填限定 annotator 模式。規格條文未變（FR-044a／AC-3.35／FR-014Q 本即禁止），僅補 Changelog 與回歸測試（13 任務 × 兩種 `run_type` 的「移除 output 欄位後面板不變」不變式，加上 annotator 側預填仍在的反向守衛）。 |
| 2026-08-17 | `annotation-015` 更新至 v3.9.0（`feat/annotation/015-review-unit-model`）：審核員模型重構 P2 之一（issue #146）。審核粒度原本隱含在 `REVIEW_MODEL_BY_RUN_TYPE` 的 run_type 分流中（`dry_run` 審共識、`official_run` 審單一標記員），使「三位標記員的標記是三個各自可審的單位」在資料層無法表達。新增 FR-051（`REVIEW_UNIT_DIMENSIONS = sample_id × annotator_id × run_type`；`REVIEW_UNIT_STATUS` 五態單欄推導，`MIN_REVIEWERS_DEFAULT = 1`）與 FR-052（逐輸出類型差異比對；`single_dim`/`multi_dim` 嚴格相等，明文不套用 `DIM_CONSENSUS_TOLERANCE`）。新增 AC-4.9~AC-4.11、SC-004K 與關鍵實體 `ReviewUnit`。本版為純加法資料模型，不移除既有條文與程式，審核卡收斂與清單粒度屬 issue #146 後續 PR。 |
| 2026-08-13 | `annotation-015` 更新至 v3.7.0（`feat/annotation/015-workspace-output-types`）：使用者回報進入 T010 醫療實體與關係辨識、T013 ABSA 的審核介面都看不到標記員標記完的結果。根因為 `official_run` 審查列一律只讀 localStorage 的真實提交，而 prototype 從未預先種入任何提交——8 個 official_run 審核任務全數受影響，span 類型因 seed 會清空實體/三元組狀態而必然空白，非 span 類型則因共用引擎的輸出角色預填而看似有值。新增 FR-044a——seed 來源優先序改為「真實提交 → `REVIEWER_MOCK_ROWS` 該樣本第一列」，遞補值走與 dry_run 共識相同的 compact 路徑（實體位置依 FR-014R 解析），不得改由資料紀錄答案欄位帶入（FR-014Q／Data Fairness）。新增 AC-6.9；連帶修訂 FR-044、FR-014J 與 `REVIEWER_MOCK_ANNOTATORS` 常數註記。 |
| 2026-08-13 | `annotation-015` 更新至 v3.6.0（`feat/annotation/015-workspace-output-types`）：使用者回報 ER+RI 合併審查卡的修正面板實體高亮為 0。根因為修正面板 seed 對每個輸出類型都無差別清空共用的 `previewEntities` / `previewTriples`，合併列先 seed `entity_recognition`、再 seed `relation_identification` 即抹除前者，`outputs[]` 含第三型別時（T013 `multi_dim`）連關係一併抹除；且共識合併值僅含 `{text, type}` 無字元位置，共用引擎不予高亮。新增 FR-014R（seed 只重置自身擁有的狀態＋自輸入文本解析位置，位置不得取自資料紀錄實體欄位）、AC-3.32。 |
| 2026-08-13 | `annotation-015` 更新至 v3.5.0（`feat/annotation/015-workspace-output-types`）：使用者回報輸出類型僅「關係識別」而不含實體辨識的任務，審核介面選取文本一律被擋下並顯示「該選取不是資料中的既有實體，請選取已高亮的實體」。根因為審查列作答面板僅以標記員提交結果 seed 實體狀態，而純 relation 任務的實體屬 evidence scaffolding、從不隨提交結果傳遞，實體清單為空使共用引擎的 `findEntitySlot()` 拒絕每一次選取（`dry_run` / `official_run` 皆然）。新增 FR-014Q——`outputs[]` 不含 `entity_recognition` 時修正面板實體狀態改以資料紀錄實體欄位為種子；含 `entity_recognition` 時維持僅由提交/共識結果 seed（Data Fairness）。新增 AC-3.31、AC-6.8。 |
| 2026-08-13 | `annotation-015` 更新至 v3.4.0（`feat/annotation/015-workspace-output-types`）：使用者回報以審核員角度檢視正式標記時，審查卡最上方重複顯示輸出類型（如 `sequence_tagging`）無資訊價值，且通過/退回按鈕與面板尾端的「無法判定 (Bypass)」分居兩處、各輸出類型決策位置不一致。新增 FR-014P——`official_run` 審查列不再渲染型別標題，通過/退回改掛於作答面板尾端 Bypass 列右側（8 種輸出類型與 span 合併列一律以同一條決策列收尾），面板重繪時重新掛回同一按鈕元素以保留決策狀態，`allow_bypass: false` 時自行補一條同樣式決策列；`dry_run` 維持型別標題不變。新增 AC-6.7；修訂 AC-6.3、AC-6.6(2)、FR-014N(3)、US6 區塊 A 與 testid 表。 |
| 2026-08-13 | `annotation-015` 更新至 v3.3.0（`feat/annotation/015-workspace-output-types`）：使用者回報同時含 `entity_recognition` 與 `relation_identification` 的任務，審核畫面原始文本重複出現三次且外觀相同、無從辨識哪一份可互動。新增 FR-014N——兩種 span 輸出類型並存時合併為單一審查列，比對資訊仍逐型別分區（`ws-review-section-{outKey}` + `ws-review-section-label`），列尾僅保留一個直接修正控件 `ws-review-correct-span`；決策、`ADJUDICATION_STATUS` 與 gold 值仍逐 outKey 獨立記錄，送出驗證不變。新增 FR-014O——「原始文本」卡收斂為僅 `dry_run` 渲染（其價值來自跨標記員聯集高亮，單標記員情境下為重複副本）。新增 AC-3.30、AC-6.6；修訂 US3 區塊 A 與 testid 表。 |
| 2026-08-12 | **IAA 策略 v2 — 全面遷移至 8-key 輸出類型分類**（`feat/annotation/015-workspace-output-types`，與 annotation-015 v3.0.0 同分支）：`dataset-017` v1.4.5 → v2.0.0（major）——IAA 框架自 legacy `task_type`/`analysis_profile` 遷移至 `outputs[]` 8-key：新增 `OUTPUT_TYPE_IAA_REGISTRY`（single_label→α nominal 0.80、multi_label→逐標籤 α 巨集平均 0.80（`IAA_LABEL_MIN_POSITIVE=5` 稀有標籤排除）、single_dim/multi_dim→ICC(2,1) 0.75/0.80、entity→span F1 strict 0.80（partial 僅展示）、relation→triple F1 0.75、sequence_tagging→token-α 0.75 + O-tag 遮罩、free_text→不計）、x/y 達標徽章（`{x}/{y} 達標 · {N} 型排除`、閘門=全部達標、`—/{y}` 待完成 Dry Run 空狀態）、`IAA_SUMMARY_STATES` + `not_applicable`、小樣本警示 `IAA_SMALL_SAMPLE_THRESHOLD=5`；`sentence_pairs` 收斂入 label/dim 四型。`task-management-014` v2.2.0：IAA 計算方式改自動選型（移除 `IAA_METHOD_ENUM` 下拉、`target_agreement_overrides` 逐型覆寫）。`task-management-013` v6.9.1：generation 建議 IAA 改不適用。`dataset-016` v2.1.0：`IAA_BADGE_STATES` + `not_applicable`。`specs/_shared/constants.md` v1.2.0 同步。 |
| 2026-08-12 | `annotation-015` 更新至 v3.0.0（`feat/annotation/015-workspace-output-types`，狀態自 `review` 回退為 `spec-ready`，breaking change）：**Reviewer 呈現依 `run_type` 分流定案**——原 AC-3.1「Dry Run 與 Official Run 皆顯示同一套通過/退回」廢止；`dry_run` 改採共識合併 + gold 仲裁模型（8 種輸出類型合併規則 FR-031~FR-035、仲裁互動與狀態機 FR-036~FR-043，新增 AC-3.20~AC-3.29），取代逐標記員通過/退回與批次全通過/全退回，不再提供「退回個人重標」通道；`official_run` 收斂為單標記員審核，新增使用者故事 6（AC-6.1~AC-6.5、FR-044）；`annotation-list` reviewer 視圖同步分流（AC-1.10、AC-1.11、FR-047、FR-048）；新增關鍵實體 `AdjudicationItem`、`GoldRecord`；新增 SC-004F~SC-004I。**同版本內兩項 Open Question 已定案並補入 Changelog**：(1) `annotation-list` dry_run 仲裁化採用——展開明細改為唯讀總覽（保留多標記員答案比對＋IAA 資訊，移除逐列/批次通過退回決策控件與 `送出審核` 按鈕），決策入口收斂為既有 `編輯` CTA 導向 workspace 完成仲裁，新增 AC-1.12、AC-1.13，修訂 FR-027、FR-029（清單送出審核邏輯廢止，ID 保留不重用）、FR-047、FR-048；`official_run` 單列模型不變；(2) `DIM_CONSENSUS_TOLERANCE` 定值為量表滿分區間 10%（`= 0.10 × (scale_max − scale_min)`），已寫入規格常數區。既有 PR #144 之實作需依新模型重新規劃 plan/tasks 方可續行；測試銜接（既有聚合審核卡測試情境重新指向 `dry_run`、helper 預設 `run_type` 更新）留待 plan 階段處理。 |
| 2026-08-11 | `annotation-015` 更新至 v2.13.0（`feat/annotation/015-workspace-output-types`，review 中）：prototype sync — 使用者要求含實體辨識輸出的審核介面，每位標記員的實體答案依標記員原本的實體列表呈現（含 token 位置與刪除功能）：新增 FR-014M——reviewer 實體答案列與 annotator「實體列表」共用同一列渲染實作（`entity-list-row`：實心型別徽章＋標記文字＋`(start, end)` token 位置＋「刪除」按鈕，刪除即時生效並重算統計；位置由紀錄實體 span 欄位渲染時以文字＋型別配對解析，同文字多次出現依答案順序取用未使用 span，無 span 欄位退回無位置列）。修訂 AC-3.4、區塊 A；新增 AC-3.19、FR-014M。 |
| 2026-08-11 | `annotation-015` 更新至 v2.12.0（`feat/annotation/015-workspace-output-types`，review 中）：prototype sync — 使用者要求 reviewer 看到的每位標記員關係答案依標記員原本的標記畫面呈現：新增 FR-014L——reviewer 關係答案列與 annotator「關係識別」清單共用同一列渲染實作（`relation-triple-row`：粗體實體文字附 `(start,end)` token 位置、觸發詞 pill、「類型：X」徽章、可操作「類型」下拉與「刪除」按鈕，統計即時重算；位置由紀錄 ner 形態三元組渲染時以主體＋客體配對解析，absa 形態退回無位置列）；「原始文本」卡觸發條件擴為 entity 或 relation（純 relation 任務高亮紀錄 evidence 實體，無 evidence 欄位顯示純文字）。修訂 AC-3.5、區塊 A；新增 AC-3.18、FR-014L。 |
| 2026-08-11 | `annotation-015` 更新至 v2.11.0（`feat/annotation/015-workspace-output-types`，review 中）：prototype sync — 使用者比對舊版 NER 審核工作區後要求還原兩項舊版視覺：新增 FR-014K——`outputs[]` 含 `entity_recognition` 時 reviewer 中欄最上方渲染「原始文本」卡，行內高亮呈現所有標記員實體結果的聯集（含任一標記員標出而其他人遺漏的實體；Bypass 列不納入），高亮帶型別色淡底＋底線＋實心型別徽章，顏色取自任務 entity config；標記員清單 entity 答案由外框 chip 改為逐行「實體類型徽章（實心色底）＋標記文字」。修訂 AC-3.4、區塊 A；新增 AC-3.17、FR-014K。 |
| 2026-08-11 | `annotation-015` 更新至 v2.10.0（`feat/annotation/015-workspace-output-types`，review 中）：prototype sync — 使用者比對舊版工作區審核卡後要求 multi_dim 每位標記員的標記結果以 `[]` 呈現並依離群值差異著色，後續補充修正 tag 樣式必須與標記清單展開明細統一：工作區聚合審核卡 `multi_dim` 標記值由逐維度 chips 改為單一 result tag（`[v1, v2, …]` 依維度序）；FR-014A 著色規則具體化（跨標記員 mean/std、Bypass 不計入，任一維度 >1.5·std 紅、>1·std 藍、其餘綠含 std=0，Bypass 列不著色）；著色演算法與清單抽出為同一單一實作來源（比照 FR-014F 前例）；workspace 與清單 result tag 視覺樣式一致。修訂 FR-014A、區塊 A、AC-3.3、AC-3.11。 |
| 2026-08-11 | `annotation-015` 更新至 v2.9.0（`feat/annotation/015-workspace-output-types`，review 中）：prototype sync — 使用者比對舊版 reviewer 標記清單後要求 multi_dim 統計改回舊版呈現、標記內容以 `[]` 顯示：`multi_dim` 統計由單行串接改為多行區塊（`mean [m1, m2, …]`、`std [s1, s2, …]` 2 位小數依維度序，其後每維度一行 `±1.5std {維度名} : lo~hi` 界值 3 位小數），維度標籤取 config 維度名稱不得硬編縮寫；因清單統計欄與工作區統計盒（FR-014F）共用單一算法來源，工作區呈現一併變更；清單展開明細 multi_dim 標記結果 tag 由鍵值對改為 `[v1, v2, …]` 陣列格式，偏差著色不變。修訂區塊 C'、AC-3.12、FR-014F。 |
| 2026-08-11 | `annotation-015` 更新至 v2.8.0（`feat/annotation/015-workspace-output-types`，review 中）：prototype sync — 使用者比對舊版 reviewer 標記清單後回報三項缺漏（標記分布統計欄、`全部退回/全部通過` 批次與逐標記員明細、toolbar `送出審核` 按鈕；consumers 遷移遺落的回歸，全部 13 任務 × 8 輸出類型還原）：統計欄算法定案為與工作區標記分布統計盒（FR-014F）同一單一實作來源（label 族 `{label}×{n}` 降冪串接、維度型逐維 `mean : m , std : s`、free_text 固定說明句、Bypass 不計入、多輸出逐行前綴型別名稱），取代舊版逐型別各異呈現條文；維度型 result tag 著色由 `[lo, hi]` 範圍改為跨標記員 mean/std 偏差規則（>1.5·std 紅、>1·std 藍、其餘綠），與工作區 FR-014A 同規則；明文化 Reviewer 視圖資料列點擊為展開/收合、導頁僅經 `編輯` 按鈕（AC-1.3、FR-008）；送出審核驗證範圍為整個任務全部筆次、決策於篩選/分頁/語言切換後保留。修訂區塊 C'、AC-1.3、FR-008、行為規則。 |
| 2026-08-11 | `annotation-015` 更新至 v2.7.2（`feat/annotation/015-workspace-output-types`，review 中）：prototype sync — 使用者回報標記清單上方缺少舊版「已完成 % · 今日筆數 · 平均速度 · 試標回合 · 本回合清單筆數」進度摘要且進度條未正常顯示（consumers 遷移遺落的回歸）；具體化任務資訊卡進度摘要組成——角色別工作統計（annotator：完成率/今日完成/平均速度；reviewer：待審筆數/進度/IAA）+ run-scoped 清單筆數，統計數值與進度百分比必須與 Dashboard 同任務、同角色列項取自同一資料來源；dry_run 筆數段為 `試標回合 R{n} · 本回合清單 {total} 筆`（無 materialized run context 時預設 R1）、official_run 為 `共 {total} 筆資料`；進度條等於完成率、全數提交覆寫 100%、查無統計時回退僅筆數段 + 0%。修訂使用者故事 1 區塊 B、FR-007C、FR-007D，新增 AC-1.8、AC-1.9。 |
| 2026-08-11 | `annotation-015` 更新至 v2.7.1（`feat/annotation/015-workspace-output-types`，review 中）：prototype sync — 使用者回報 ER+RI 整合模式的整合預覽外框在標記卡內形成重複框線、畫面雜亂；整合預覽區塊改為不再包外框，卡片邊框為唯一外框（延伸 v2.4.0 規則）。修訂 FR-013E、AC-2.12。 |
| 2026-08-11 | `annotation-015` 更新至 v2.7.0（`feat/annotation/015-workspace-output-types`，review 中）：使用者回報關係識別標記介面與 task-new Step 2／task-detail 標記設定差距過大——工作區關係建構器（純模式與 ER+RI 整合模式，reviewer 直接修正依 FR-024L-1 重用同控件）定案為與 task-new Step 2 相同的循序建構器：原始文本反白選取後依序按 `E1/Arg1 → Relation → E2/Arg2` 填入草稿欄位（含 E1/Rel/E2 草稿狀態欄與當前選取顯示），按「新增」入列；E1/E2 僅接受既有實體相符選取（不符顯示錯誤）、Relation 接受任意選取、按鈕依草稿進度循序啟用；「退回」為草稿逐格撤回（E2→Rel→E1）、已入列三元組逐列刪除、`relation` 語意類型改為入列後經類型選單事後指定的選填欄位。移除 spec-only 從未實作的 E1==E2 阻擋與舊「Undo 移除最後一筆 triple」語意。修訂 AC-2A.7、FR-024C、FR-024C-1、FR-024C-2、邊界情況。 |
| 2026-08-11 | `annotation-015` 更新至 v2.6.2（`feat/annotation/015-workspace-output-types`，review 中）：prototype sync — 使用者回報 annotator 標記介面 single_dim/multi_dim 滑塊上方顯示「—」而非數值：當前值標籤於任何時刻皆顯示滑桿當前數值、與 number input 一致（Output-role preannotation 依 FR-024M 顯示實際值，無 preannotation 時顯示範圍中點起始位置），唯 Bypass 勾選中顯示未評分佔位符號；「未作答」判定移至提交閘門，起始中點不計為已作答。修訂 AC-2A.3、AC-2A.4、FR-024F、FR-024G、SC-008。 |
| 2026-08-11 | `annotation-015` 更新至 v2.6.1（`feat/annotation/015-workspace-output-types`，review 中）：prototype sync — 使用者回報 annotator 標記介面 multi_label 兩項缺陷修正：(1) 階層選擇器展開時為標記卡最上層不透明覆蓋層、完整遮蓋後方內容（含 Bypass 勾選項），移除 prototype 將 Bypass 抬升至選擇器之上的堆疊處理（原造成 Bypass 穿透疊在選擇器標題上），與 task-new Step 2 呈現一致；(2) `max_selections` 上限提示文字沿用工作區既有欄位提示樣式。修訂 FR-024E、FR-024E-1、AC-2A.2，新增 AC-2A.2a。 |
| 2026-08-11 | `annotation-015` 更新至 v2.6.0（`feat/annotation/015-workspace-output-types`，review 中）：還原右欄「說明與檔案」面板舊版視覺——`任務說明` 摘要標題加回提示圖示（info circle icon）；檔案列表每列加回檔案類型小圖示（PDF/圖片/Markdown 三型、類型色彩區分）與檔名右側動作提示文字（PDF → `新分頁`、圖片/Markdown → `預覽`，i18n 隨語言切換即時更新）；圖示與動作提示由 `GuidelineFile.type` 驅動、不得依任務別硬編，維持 `guidelineFiles` 動態渲染架構（僅還原視覺，非回退靜態清單）。新增 FR-020B、AC-5.4，使用者故事 3 區塊 C 補規範（annotator/reviewer 共用面板）。 |
| 2026-08-10 | `annotation-015` 更新至 v2.5.0（`feat/annotation/015-workspace-output-types`，review 中）：**US3 聚合審核卡具體化**（一致性 + 具體化修訂，非新增功能——原 US3 已要求逐標記員列/批次操作/歷程追溯，本版補齊 prototype 落後既有契約的部分）：新增審查列固定結構順序與 `ws-review-*` testid 契約（`ws-review-row`/`ws-review-stats`/`ws-review-note`/`ws-review-bulk-reject`/`ws-review-bulk-approve`/`ws-review-annotator-list`/`ws-review-annotator-row`/`ws-review-annotator-name`/`ws-review-annotator-answer`/`ws-review-row-reject`/`ws-review-row-approve`/`ws-review-submit-btn`，FR-014E，AC-3.11）；標記分布統計改為渲染時依標記員清單計算，逐型別公式明文化（FR-014F，AC-3.12）；批次按鈕新增 `aria-pressed` 僅全列同值時為真的規則（FR-014G，AC-3.13）；「送出審核」新增全 outKey × 標記員決策完整性驗證與 toast 阻擋（FR-014H，AC-3.14）；「目前標記員」列被退回時落實 annotator bucket 狀態回退為待標記（保留原答案）並新增紅色徽章歷程事件（FR-014I，AC-3.15）；新增固定模擬標記員資料模型 `REVIEWER_MOCK_ANNOTATORS`／`ReviewerMockRow（Prototype）`，涵蓋 13 個任務全樣本 × 8 種輸出類型（FR-014J，AC-3.16）；`ReviewDecision` 對齊為「標記員 × 輸出類型」決策維度。FR-027–029（annotation-list 送出審核，未實作）維持不動。 |
| 2026-08-10 | `annotation-015` 更新至 v2.4.0（`feat/annotation/015-workspace-output-types`，review 中）：移除工作區中欄卡片內多餘框線與隔線——題目卡內 input 內容不再包內框（engine 預覽內框於卡片內為重複框線）、標記卡內移除殘留水平分隔線與 Bypass 選項上方虛線隔線（Bypass 自身虛線外框保留）；以 workspace scoped 樣式覆寫落實、一體適用全部輸出類型、不動共用 engine（FR-013E、AC-2.12）。 |
| 2026-08-10 | `annotation-015` 更新至 v2.3.0（`feat/annotation/015-workspace-output-types`，review 中）：v2.2.0 移除工作區中欄任務標題卡（任務名稱改由右欄任務說明摘要呈現、語言切換改綁共用 sidebar，AC-2.7/AC-3.10、FR-014C/FR-014D）；v2.3.0 依使用者定案還原 pre-outputs[] 工作區 chrome（annotator/reviewer 一致）——左欄每筆樣本三態完成狀態標籤（已提交/已儲存/待標記）、中欄頂部樣本導覽列（上一筆/下一筆 + 已提交筆數/總筆數進度）、題目卡與標記卡獨立區隔、底部 autosave 狀態列 + 儲存草稿鈕（FR-013A–D、AC-2.8–2.10）；`GUIDELINE_PANEL_TABS` 由 v2.1.0 暫定的 `guideline-files-static` 定案回滾為 `guideline-files \| history` 雙頁籤，歷程頁籤合併 annotator/reviewer 事件時序（FR-016B、AC-2.11），對應 Open Question 結案。 |
| 2026-08-10 | `annotation-015` 更新至 v2.1.0 並於 `feat/annotation/015-workspace-output-types` 進入 `review`：v2.0.0 全面重寫——workspace/list 自舊 5 型 taxonomy 遷移至 `outputs[]` 8-key 組合模型，URL 契約破壞性改為 `task_id`/`sample_id`/`role`/`run_type` 4 參數（移除 `task_type`/`sub_type`），標記面板 registry-driven 重用 task-config engine，Reviewer 直接修正涵蓋全 8 型並以 annotator 答案 seed；v2.1.0 實作後一致性修訂——新增 FR-024M/FR-024M-1（Output-role prefill 全 8 型 + Data Fairness 未映射欄位剝除，依 013 FR-003g-5），FR-026/FR-024F/FR-024G/AC-2A.4 加例外字句，`GUIDELINE_PANEL_TABS` 修訂為右欄常駐無 tab（pending user confirmation），TaskProfile 新增 `guidelineFiles` 通用建模。dashboard.js／dashboard.assignments.js 消費端同步遷移 4/3 參數 URL。 |
| 2026-07-31 | `task-management-014` 更新至 v2.1.0（`fix/free-text-preview-card-title`）：同步 `013-task-new` v6.9.0 項目對名稱——輸入類型為 `item_pair` 時「標記設定」編輯模式呈現「項目對名稱」設定卡（013 FR-003k 同構），「基本資料」／「標記設定」儲存時持久化生效名稱並於重新進入編輯模式帶回，「基本資料」更換資料集後名稱以新資料集重新初始化；新增 FR-014l-3。 |
| 2026-07-31 | `task-management-013` 更新至 v6.9.0（`fix/free-text-preview-card-title`）：輸入類型為 `item_pair` 時，Step 2 標記設定於手風琴清單上方新增不可收合的「項目對名稱」設定卡，兩欄位預設帶入 Step 1 兩個 Input 欄位的原始欄位名稱並開放編輯；預覽配對區塊小標即時顯示生效值，清空回退原始欄位名稱；unified config 新增頂層 `item_pair_labels: [string, string]` 並於 Code 儲存回填驗證；014 經共用引擎同步生效並於儲存時持久化。新增 FR-003k 與驗收情境 32，更新 FR-003g-3 與 Prototype Playwright 斷言。 |
| 2026-07-31 | `task-management-013` 更新至 v6.8.0（`fix/free-text-preview-card-title`）：`sequence_tagging` 於 `OUTPUT_TYPE_REGISTRY` 宣告 `hidePreviewTitle: true`，Step 2 標記預覽不再顯示輸出卡片的「序列標註」標題，預覽直接以「原始文本」區塊起始；014 標記設定編輯模式經共用引擎同步生效。更新驗收情境 10、FR-003d-1 與 Prototype Playwright 斷言。 |
| 2026-07-31 | `task-management-013` 更新至 v6.7.0（`fix/free-text-preview-card-title`）：`multi_label` 的「所有層級皆可選 — 父、子標籤可分別勾選，系統會保留其分類位置。」提示自 taxonomy 樹編輯器內的固定提示框（`.taxonomy-editor-note`）改為「標籤選項」欄位標題旁實心圓形「?」按鈕的 tooltip（registry `hintAsTooltip: true` + `hint_zh`／`hint_en`，樣式同 v6.6.0）；共用引擎抽出 `attachFieldHintTooltip` helper 供 text 與 taxonomy-tree 欄位共用；014 標記設定編輯模式經共用引擎同步生效。 |
| 2026-07-31 | `task-management-013` 更新至 v6.6.0（`fix/free-text-preview-card-title`）：`free_text` 的「輸入區說明」／「作答區說明」helper text 改為欄位標題旁實心圓形「?」按鈕的 tooltip（registry `hintAsTooltip: true`，hover／focus 顯示），泡泡樣式對齊 MASTER.md §Tooltip（深色、觸發點上方、帶箭頭），樣式集中於共用 `task-config.css` 並移除頁內重複定義，task-new／task-detail 抽樣筆數提示同步套用；014 標記設定編輯模式經共用引擎同步生效。 |
| 2026-07-31 | `task-management-013` 更新至 v6.5.0（`fix/free-text-preview-card-title`）：`free_text` 於 `OUTPUT_TYPE_REGISTRY` 宣告 `hidePreviewTitle: true`，Step 2 標記預覽不再顯示輸出卡片的「自由文字」標題，並移除預覽內兩條分隔線（Evidence 區塊後、Input 內容卡與作答區之間），區塊間距改以 spacing 維持；014 Overview「標記設定」編輯模式經共用引擎（task-config.\*）同步生效，014 spec 以 parity 引用不需個別修改。更新驗收情境 26、FR-003d-10 與 Prototype Playwright 斷言。 |
| 2026-07-31 | `task-management-014` 更新至 v2.0.0 並於 `feat/task-detail-config-sync` 進入 `in-progress`：任務詳情自 legacy `task_type` 枚舉遷移至 ADR-029 `categories[] + input_types[] + outputs[]` 組合模型，與 013 共用設定引擎與 `OUTPUT_TYPE_REGISTRY`；「基本資料」編輯改為 Step 1 同構（資料集檔案列＋欄位角色表＋三組 chips）、「標記設定」編輯改為 Step 2 同構（每 output 一個 accordion＋同源預覽＋YAML/JSON code 區）；FR-014k–u 汰換為 FR-014k/l/l-1/l-2/m/n，AspectListTaskConfig／SentencePairsTaskConfig 併入 registry 驅動之 OutputConfig；匯出 `task_type` 改由 outputs[] 推導 legacy enum。同步 `task-management-010` v2.0.2：13 筆示例 seed 統一 `status = draft` 起始。 |
| 2026-07-29 | `dashboard-012` 更新至 v2.0.2：記錄 legacy `localStorage` 鍵 `labelsuite.activeTaskType` 的過渡性寫入——Dashboard 為目前唯一寫入者（010 v2.0.0 已移除），值僅取自獨立 `task_type` compatibility 欄位，014／015 遷移完成後一併移除。原 `feat/task-output-type-list` 分支依 PR 大小規範拆分為 `feat/task-list-output-types`（010／013）、`feat/dataset-analysis-output-types`（016）、`feat/dashboard-output-types`（012）三個堆疊 PR。 |
| 2026-07-29 | `dashboard-012` 更新至 v2.0.1：Annotator 與 Reviewer prototype 場景皆補齊 T001–T013 完整清單，每筆保留獨立 task ID、可操作 sample 與明確 compatibility route，可分別載入標記／審核介面；13 筆仍只作 prototype 驗收基線，正式產品依 membership 顯示且不限制未來任務數量或輸出組合。 |
| 2026-07-29 | `dashboard-012` 更新至 v2.0.0 並於 `feat/task-output-type-list` 進入 `in-progress`：四種有任務角色的 Dashboard 任務摘要改由 `outputs[].type` 依序顯示一至多個 registry-driven tag，涵蓋 8 個合法 key、複合輸出、zh/en、可存取名稱與手機換行；13 筆 fixture 僅為安全 summary metadata 的 prototype 基線，第 14 筆任意合法組合不需新增 renderer 分支，並明訂答案資料不得暴露。同步 `task-management-013` v6.4.3；014／015／017 consumer 延後範圍不變，legacy `task_type` 僅保留為獨立 routing compatibility 欄位。 |
| 2026-07-29 | `dataset-016` 更新至 v2.0.0 並於 `feat/task-output-type-list` 同步 prototype：資料集分析列表改以 `outputs[].type` 顯示一至多個唯讀 tag，下拉由 registry 提供 8 個輸出類型並採 membership 篩選，URL 分頁改為 `limit`／`offset`；13 份 example-data fixture 僅為可驗收示例，不構成任務數量或合法組合上限，並新增資料載入失敗、權限過濾、14th-task 泛化與三個 RWD viewport 驗收。同步 `task-management-010` v2.0.1、`task-management-013` v6.4.2 的下游界線；017 detail consumer 仍延後。 |
| 2026-07-29 | `task-management-010` 更新至 v2.0.0 並於 `feat/task-output-type-list` 進入 `in-progress`：任務列表由固定 `task_type` 遷移為 `outputs[].type` 多 tag，輸出類型下拉由 registry 提供 8 個合法 key 並採 membership 篩選，URL 分頁改為 `limit`／`offset`；以 13 份 example-data fixture 建立 prototype mapping／命中數基線、複合 tag 與第 14 筆合法任務泛化驗收，明訂示例不得限制未來任務或暴露答案內容。`task-management-013` 釐清 Step 1 狀態為 `selected_categories[]` + `input_type` + `selectedOutputTypes[]` 並更新至 v6.4.1；014／015／016／017 consumer 同步仍延後。 |
| 2026-07-28 | `task-management-013` 更新至 v6.4.0：Step 2 `sequence_tagging` 預覽將 Token 網格上方的字／詞切分規則說明改為顯示帶「原始文本」標題（英文 Text）、未經切分的原始輸入文本，文本不隨標記單位切換改變；通用輸入文字區塊標籤由 Input 欄位名稱改為「原始文本」，`item_pair` 於配對區塊上方顯示一次該標題並保留欄位名稱小標，互動圈選文本區與 `free_text` `input_instruction` 契約不變，整合預覽標題簡化為「整合預覽」；同步 prototype、visual overview 與 Playwright 測試，tokenization 契約與預標記驗證不變。 |
| 2026-07-28 | `task-management-013` 更新至 v6.3.0：`sequence_tagging` 新增獨立的標記單位設定 `tokenization.unit = character \| word`，設定面板依序顯示標記單位與標記方案；Step 2 預覽依選定單位即時重建字／詞 Token、更新規則說明並重新驗證可見預標記數量。`tokenization` 契約升級為 unit-based v2；正式 Annotation Workspace 及 014／016／017 consumer 維持延後。 |
| 2026-07-28 | `task-management-013` 更新至 v6.2.0：本次僅調整 New Task producer-side 的 `sequence_tagging` 設定與 Step 2 預覽；Token 固定採 language-aware v1（中文逐字、英文逐詞、標點獨立），方案保留 BIO／BIOES／IOB2 並新增 SINGLE，完整 tag 可精確套用且可見預標記數量不一致時阻擋。同步 example data、task config、taxonomy、visual overview 與 Playwright；正式 Annotation Workspace 及 014／016／017 consumer 留待後續一起調整。 |
| 2026-07-28 | `task-management-013` 更新至 v6.1.0 並進入 `in-progress`：Evidence 欄位新增全資料完整性結果與缺值阻擋；`free_text` 新增必要且支援 zh/en 預設的輸入區／作答區說明，設定值即時同步預覽與 Code，主要標題不再暴露原始 JSON key；舊 config 會補 instruction 並清除 v6.0.0 退役 key。Prototype 與 Playwright 回歸測試同步於 `feat/task-management/013-free-text-guidance`；014／015 consumer 契約留待後續同步。 |
| 2026-07-24 | `task-management-013` 更新至 v6.0.0：破壞性移除 `free_text.show_reference`、舊名 `show_reference_to_annotator` 與「顯示參考答案給標記者」設定；指定 Evidence 時，Step 2 依「背景參考 (Evidence) → Input → 回答框」顯示，回答區只保留「自由文字」標題，Output 角色資料直接預填回答框，未指定 Output 時保持空白。同步 producer-side prototype、Playwright、`rendersEvidencePreview` metadata 與 ground-truth 安全界線；014／015／017 consumer 同步維持延後。 |
| 2026-07-24 | `task-management-013` 更新至 v5.2.1：v5.1.0 階層選擇器攤平與保持開啟、v5.2.0 taxonomy 分支刪除確認改用 UXC-10 頁內 modal、v5.2.1 釐清下游延後範圍僅限 014／015／017 consumer 端（同步修正 ADR-029）。producer-side prototype 已於 `feat/hierarchical-multi-label-taxonomy` 實作（樹編輯器、攤平選擇器、刪除確認 modal、資料路徑與 `max_selections` 驗證），更正 v5.0.0 列「本階段不修改 prototype」的描述。 |
| 2026-07-24 | `task-management-013` 更新至 v5.0.0 並調整為 `spec-ready`：`multi_label.label_options` 改為 bounded recursive taxonomy，新增 `taxonomy-tree`、stable ID、leaf-only preview、flat／hierarchical shape-aware 正規化、8 層／500 節點／100 字元資源界線及 JSON/YAML、鍵盤、RWD 驗收規劃；保留原 flat fixture，另規劃 hierarchical fixture。014／015／017 consumer 顯示、提交與統計契約延後同步，本階段不修改 prototype 或程式碼。 |
| 2026-07-24 | `task-management-013` 更新至 v4.9.0：Step 2 的 `relation_types` 改為選填並預設空陣列；純關係與所有相關複合任務僅在存在語意類型標籤時顯示「類型」控制，純關係預覽移除重複標題；狀態維持 `in-progress`。 |
| 2026-07-23 | `task-management-013` 更新至 v4.7.0：單／多維度回歸設定統一為相同維度卡片與欄位排列；單維度固定一張且無新增／刪除，多維度保留卡片清單操作並移除外層「維度設定 *」重複標題，既有 config 與 `outputs[]` 契約不變。 |
| 2026-07-23 | `task-management-013` 更新至 v4.6.0、`annotation-015` 更新至 v1.7.0：右側 number input 採 `step="any"`，範圍內小數不再依 slider step 吸附；slider 仍依 task config step 微調，手動輸入僅依 min/max 校正。 |
| 2026-07-23 | `task-management-013` 更新至 v4.5.0、`annotation-015` 更新至 v1.6.0：回歸滑桿右側改為 number input，與滑塊及上方數值標籤雙向同步；完成輸入後依 min/max/step 校正並移動滑塊，資料契約不變。 |
| 2026-07-23 | `task-management-013` 更新至 v4.4.0、`annotation-015` 更新至 v1.5.0：單／多維度回歸統一採 range slider，數值即時跟隨滑塊顯示於正上方；多維度使用不同輔助色並保留文字辨識，annotation pending 樣本維持未評分契約。兩項進入 `feat/regression-slider-ui` prototype 實作，狀態更新為 `in-progress`。 |
| 2026-07-23 | `task-management-013` spec 更新至 v4.3.0：全部 8 種輸出類型、單一輸出及多輸出組合統一採 Step 2 設定優先版面（>1100px 設定左／預覽右，≤1100px 設定上／預覽下），左右小標同層級頂端對齊；範本／上傳與 Code 固定於下方共用單一外框及分隔線，Code 為 240px，各 output 的 Bypass 與前一 schema 欄位保留 12px。版面改為頁面層級共通契約；`outputs[]` 不變，已檢查下游 014–017 無需改版，狀態維持 `spec-ready`。 |
| 2026-07-23 | `task-management-013` spec 更新至 v4.2.1：設定優先 Step 2 新增「標記設定」小標並與「標記預覽」維持相同樣式及頂端對齊；共通 Bypass schema toggle 與前一欄位保留 12px token-based 群組間距；同步 FR-003a-3、FR-003j、SC-003h／SC-003k 與 prototype 驗收，`outputs[]` 契約及下游規格不變，狀態維持 `spec-ready`。 |
| 2026-07-22 | `task-management-013` spec 更新至 v4.2.0：導入 Step 2 設定優先響應式版面與下方整合設定檔工具卡初版；適用範圍後由 v4.3.0 統一為全部輸出類型及多輸出組合，狀態維持 `spec-ready`。 |
| 2026-07-22 | `task-management-013` spec 更新至 v4.1.0：Step 1 分類（單一標籤／多標籤）與回歸（單維度／多維度）輸出改為組內 radio 單選，選擇語意由 taxonomy `outputSelection` metadata 驅動；序列維持 checkbox 多選、跨大分類仍可多選；prototype 與 SC-002e 測試同步，狀態維持 `spec-ready`。 |
| 2026-07-22 | `task-management-013` spec 更新至 v4.0.0：從新增任務 Step 1／Step 2 與 registry 移除 `entity_relation`、`boundary`；將 `span`、`relation_triple`、`token_class` config key 破壞性遷移為 `entity_recognition`、`relation_identification`、`sequence_tagging`，不保留相容別名，並同步顯示名稱、taxonomy、config 範例、prototype、visual overview 與回歸測試；狀態維持 `spec-ready`。 |
| 2026-07-15 | `task-management-013` spec 同步至 v3.4.3：純 `relation_triple` 僅保留既有實體唯讀高亮、關係建構器與三元組列表，不顯示 Span 編輯介面或 `source_output`；只有明確選擇 `span + relation_triple` 才啟用整合實體編輯並輸出 `source_output: span`；同步驗收情境、功能需求、registry 契約、產品 taxonomy 與範例 task config；狀態維持 `spec-ready`。 |
| 2026-07-15 | `task-management-013` spec 同步至 v3.4.2：新增 `rendersInputPreview` registry UI metadata；`span`、`relation_triple`、`entity_relation` 與相關複合任務不再重複顯示通用輸入區，`token_class`、`boundary` 維持既有顯示；補齊驗收情境、介面與行為規則、邊界情況、FR-003g-3、關鍵實體、下游相依性與 SC-003j；狀態維持 `spec-ready`。 |
| 2026-07-13 | `task-management-013` spec 同步至 v3.4.1：Boundary 預覽新增依 `boundary_types` 順序配置的類型色系、SVG 剪刀與類型首字縮寫、完整類型可存取名稱、已標記切點防遮蔽間距，以及 `sentence`／`paragraph` 階層式即時切割結果；補齊預覽互動表、邊界情況、FR-003d-2 與 SC-003i；狀態維持 `spec-ready`。 |
| 2026-07-06 | `task-management-013` spec 更新至 v3.2.0：每個輸出類型新增獨立「無法判定 (Bypass)」選項——registry 共通欄位 `allow_bypass`（預設開啟）、預覽 Bypass 勾選項與互斥清空停用行為、整合預覽（span + relation_triple）前綴勾選項與連鎖停用邊界（新增 FR-003j、SC-003h）；prototype 同步；狀態維持 `spec-ready`。 |
| 2026-07-06 | `task-management-013` spec 同步至 v3.1.7（PR #93 合併）：資料列來源偵測與髒 JSON 解析、預覽 Modal 回退鏈、循序關係三元組建構器、來源切換不相容提示等 prototype 行為入 spec；狀態維持 `spec-ready`（實作尚未開始，不觸發封存）。 |
| 2026-06-09 | Update account-001 plan to v2.0.0: full alignment with plan-template v1.13.6; status confirmed `plan-ready`. |
| 2026-06-09 | Update foundation-000 plan to v2.0.0: aligned with plan-template v1.13.6 (major structural update). |
| 2026-06-05 | foundation-000 plan v1.0.0 created (Foundation-Core): plan-ready; scope F-01~F-10, F-13, F-16, F-18; Observability/Celery deferred; health check endpoint added. |
| 2026-06-04 | Update foundation-000 to spec v1.12.0: pagination switched from page/page_size to limit/offset; PaginatedResponse next_offset added. |
| 2026-06-03 | Update foundation-000 to spec v1.11.5: SC-045 naming change applied. |
| 2026-06-03 | Update foundation-000 to spec v1.11.4: add ADR-024 upstream dependency (tiered database strategy — SQLite quick start / PostgreSQL production). |
| 2026-06-02 | Update foundation-000 to spec v1.11.3: address PR review-resolve findings — replace Django-specific override_settings in SC-036 with FastAPI-compatible pytest monkeypatch / dependency_overrides; realign branch field to feat/foundation/000-foundation per naming convention. |
| 2026-06-02 | Update foundation-000 to spec v1.11.2: address PR code review findings — fix FR-040 parenthetical self-contradiction, add ADR-022 upstream dependency, backfill SC-029~031 changelog attribution. |
| 2026-06-01 | Update foundation-000 to spec v1.11.1: apply independent-review corrections — clarify FR-117/FR-078 CSRF coexistence, add explicit operation_id= guidance to FR-120, add FR-040/FR-088 supersede notes, fix SC-034/SC-041 three-viewport contradiction, add CSRF test attribution to SC-036, add metrics grep pattern to SC-022, add Celery headers assert to SC-039, add bootstrap CI check path to SC-045. |
| 2026-06-01 | Update foundation-000 to spec v1.11.0 on `feat/foundation-spec-review-fixes`: resolve sub-agent review findings for ErrorResponse P0 contract, Celery baseline, CSRF, restricted-client safety, API versioning/idempotency, REST non-CRUD patterns, frontend gates, correlation, metrics naming, local bootstrap, and expanded CI success criteria. |
| 2026-05-29 | Update foundation-000 to spec v1.10.0: add Prometheus / Grafana / Sentry observability baseline, FR-091~100, SC-021~028, and ADR-018/019/020 dependencies. |
| 2026-05-29 | Update foundation-000 to spec v1.7.0: add architecture background mapping SRP/OCP/LSP/ISP/DIP/CARP/LKP to Foundation constraints. |
| 2026-05-29 | Update foundation-000 to spec v1.1.0: add F-21 Backend Layering (FR-060~062), F-22 Frontend Vertical Slice (FR-063~065), Filtering/Sorting convention (FR-059), SC-009/010. |
| 2026-05-29 | Add foundation-000 Foundation Spec v1.0.0 (spec-ready); 20 constraints FR-001~058; ADR-021/022 created. |
| 2026-05-28 | Update account-001 status to `plan-ready`. |
| 2026-05-22 | `admin-006` User Management spec 更新至 v1.0.8；同步最新 prototype 的列內「異動紀錄」icon、目標帳號異動紀錄 drawer、空狀態、i18n 與行動版 bottom sheet 行為。 |
| 2026-05-22 | `admin-006` User Management 經 `/speckit.clarify` 更新至 spec v1.0.7；補齊啟用帳號、seeder/最後 active super_admin 保護、自停用導頁、帳號管理審計與設定密碼信寄送失敗不建立帳號規則。 |
| 2026-05-22 | `account-005` Profile Settings 經 `/speckit.clarify` 更新至 spec v1.2.9；補齊頭像、通知偏好、Email 驗證 session、驗證信重送與 pending Email 規則。 |
| 2026-05-21 | 依各 `spec.md` 檔頭同步 `STATUS.md`：更新功能分支為 `feat/[module]/NNN-feature` 格式、補齊 spec 版本與規格狀態；`shared-018` 標記為 `deferred`，`dataset-016` 保持 `in-progress`。 |
| 2026-05-19 | 依最新 prototype 同步 IA 與相關 spec：task-new 4 steps、task-detail 5 tabs、dataset `/dataset-analysis` 入口、admin `role-settings.html` 獨立頁；shared-018 標記為 deferred。 |
| 2026-05-15 | 新增 `shared-018` Help Button spec（`specs/shared/018-help-button/spec.md`）；狀態設為 `spec-ready`。 |
| 2026-05-12 | `task-management-014` spec 更新至 v1.7.6；Overview「任務狀態與執行控制」移除 stage banner 內額外的目前任務階段標題與描述，僅保留判定列。 |
| 2026-05-11 | `task-management-014` spec 更新至 v1.7.5；Overview「任務狀態與執行控制」統整目前任務階段與正式標記判定資訊架構，prototype 同步移除獨立正式標記判定卡。 |
| 2026-05-06 | `task-management-014` 狀態更新為 `in-progress`，分支為 `fix/task-detail-annotation-results-layout`；spec 同步目前 `annotation-results` prototype 的摘要頂對齊、detail row metadata 群組與 scroll container 邊界規則。 |
| 2026-04-30 | `dataset-016` 狀態更新為 `in-progress`，分支為 `feat/dataset-analysis-table-refresh`；spec 同步目前 prototype 的 table layout、篩選器、分頁與 URL query 保留行為。 |
| 2026-04-24 | 重命名 dataset spec 資料夾為 `016-dataset-analysis-list`、`017-dataset-analysis-detail`；同步對齊 IA 的 `dataset-analysis-list` 與 `/dataset-analysis-detail/:task_id` 命名。 |
| 2026-04-24 | 新增 `dataset-016` 與 `dataset-017` 規格檔（IA v1.3.1 重建）；狀態更新為 `spec-ready`，並自 backlog 移除。 |
| 2026-04-23 | 新增 `annotation-015` 規格檔（`specs/annotation/015-annotation-workspace/spec.md`）；狀態更新為 `spec-ready`，並自 backlog 移除。 |
| 2026-04-20 | 新增 `task-management-010`、`task-management-013`、`task-management-014` 規格檔；狀態更新為 `spec-ready`，並自 backlog 移除。 |
| 2026-04-16 | 新增 `admin-006` 與 `admin-007` 規格檔（IA v7 重建）；狀態更新為 `spec-ready`，並自 backlog 移除。 |
| 2026-04-16 | 已同步 STATUS 與目前 repository 狀態：自 active pipeline 移除已刪除 spec 項目、加入 `shared-001`，並將 `006/007/010/013/014/015/016/017` 移至 backlog 等待重建。 |
| 2026-04-15 | 將 `001/002/003/004/012` 狀態由 `spec-ready` 更新為 `in-progress`；分支設定為 `feat/dashboard-012-spec-simplify`；並同步備註與最新 account/dashboard spec 及 IA 對齊進度。 |

---

## 封存紀錄

> 功能資料夾移至 `specs/_archive/` 後，請在此新增紀錄。

| # | 功能 | 封存日期 | 合併 PR |
|---|------|----------|---------|
| — | — | — | — |
