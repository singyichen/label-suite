# 任務清單：exception-pool-select-then-confirm

> **範圍硬閘**：只得修改 `design/prototype/**`、本 change 與正典／治理檔；不得修改 `backend/**`、`frontend/**` 或 root `e2e/**`。Apply 前執行 OpenSpec schema validation 與 Project SDD lint。

## 1. PR-920A — 最終例外處置改為先選取、後確認，理由必填擴及四種處置

> **相依與平行性**：1.1 → 1.2 → 1.3 → 1.4 → 1.5 嚴格依序。本 change 只修改 `annotation-workspace.config.js` 的 `buildExceptionPoolItemRow()`／`expandExceptionPoolAction()` 與其內嵌 i18n 區塊，與同波並行的 #956（`buildUnits()` 約 :1625）、#992（`handleReviewSubmit()` 約 :5483）不同區域，可並行 apply；本 issue 排在群組最後合併。

**故事目標**：延續 issue #907 已落地的例外池外殼（AC-4.69、SC-011）與 2026-09-24／2026-09-26 兩則維護者裁定，把最終例外處置的四個動作由「一鍵即定案」改為「先選取、後確認」的單一寫入點，理由必填擴及全部四種處置，並修正 `adopt_annotator`／`adopt_reviewer` 現行 `reason: ''` 的缺陷（issue #913）。新增可量測成功標準 **SC-012**（理由必填未通過次數與確認按鈕誤啟用次數皆為 0），於 1.4/1.5 archive 回寫時寫入正典「成功標準」清單。

- [x] 1.1 以既有主要契約檔 `design/prototype/tests/annotation/issue-596-exception-pool.spec.ts` 為主，將「一鍵即處置」之斷言改判為前提消失並改寫為兩段式正向斷言（點選處置按鈕 → 斷言僅標記選取、未寫入、`aria-pressed` 為 `true` → 填理由 → 點統一確認按鈕 → 斷言寫入完成且 `reason` 非空）；新增彙整列（`ws-exception-pool-summary`）四種狀態與確認按鈕（`ws-exception-pool-confirm`）disabled/enabled 狀態的斷言；`custom_answer`／`exclude_from_dataset` 之空理由阻擋斷言改為檢查按鈕 `disabled` 屬性而非點擊後跳 toast。同步改寫三個間接依賴一鍵寫入或舊確認 testid 的既有測試（`tests/task-management/issue-891-live-review-pools.spec.ts`、`tests/annotation/issue-913-exception-pool-sticky-owner.spec.ts`、`tests/annotation/issue-914-finalized-value-validation.spec.ts`）之驅動序列，其最終寫入結果斷言本身不變。建立 committed Red，跑出預期失敗並記錄。[@senior-qa]
  - Red commit：`faaf665f`。`PW_PORT=8986 pnpm playwright test`（4 檔）→ 本機獨立複驗 11 passed／9 failed，與 subagent 報告數字一致；9 個失敗逐一核對皆為預期失敗（`aria-pressed` 屬性不存在、`ws-exception-pool-summary`／`ws-exception-pool-confirm` testid 不存在、舊 testid 已移除造成 timeout），非選擇器誤植。`git show --stat faaf665f` 確認僅 4 個測試檔、無生產碼變更。
- [x] 1.2 Green：於 `design/prototype/pages/annotation/annotation-workspace.config.js` 改寫 `buildExceptionPoolItemRow()`／`expandExceptionPoolAction()`：四個處置按鈕點擊只切換本地 `selectedAction`（`aria-pressed`），不再呼叫 `resolveFn`；恆常渲染理由欄、新增彙整列、統一確認按鈕（原生 `disabled`，見 design.md D3）；`custom_answer` 之 `expandHost` 加 delegated click listener 供彙整列即時更新（design.md D4）；新增 i18n 鍵 `exceptionPoolNoSelectionLabel`／`exceptionPoolSummaryTpl`／`exceptionPoolSummaryWithValueTpl`，移除孤兒鍵 `exceptionPoolReasonRequired`（zh／en）。不得弱化 1.1 之 Red 契約。[@main]
  - Green commit：`2633d634`。同一批 4 檔測試本機獨立複驗 → **20 passed／0 failed**；額外三檔外殼煙霧測試（`issue-907`／`issue-815`／`annotation-review-flow-demo-seed`）23 passed。`git show --stat 2633d634` 確認僅生產碼一個檔案、`+145/-75`。Red 契約逐條保留未弱化。
- [x] 1.3 執行驗證閘門：`pnpm typecheck`（clean）；以 `grep -rl` 推導之候選集（17 檔，見下）分批執行 Playwright（PW_PORT=8986）→ 全數通過；`node scripts/gen-screen-inventory.mjs` 重生一次（commit `cc17551d`）後 `--check` 通過；`scripts/inventory-tests.sh` 11/11 PASS；`scripts/check-sdd.sh` → 0 error(s)／15 warning(s)（皆既有 baseline 或本 change 預期的 review-required 標記）；`scripts/check-spec-artifacts.sh` → passed；`node scripts/check-user-path-map-freshness.mjs` → `PATH_MAP_FRESH`。全部通過。[@main]
  - 候選集推導（`grep -rl "ws-exception-pool-action\|exception-pool\|exceptionPool"` ∪ `grep -rl "adopt_annotator\|adopt_reviewer\|custom_answer\|exclude_from_dataset"` ∪ `grep -rl "FR-095\|AC-4\.56\|AC-4\.57"`，皆於 `tests/`）→ 17 個檔案。其中 4 檔為本 change 改寫（issue-596-exception-pool／issue-891-live-review-pools／issue-913-exception-pool-sticky-owner／issue-914-finalized-value-validation，20 passed）；其餘 13 檔（annotation-review-flow-demo-seed／issue-562-review-action-hint-removed／issue-596-arbitration／issue-596-finalized-card／issue-596-review-unit-status／issue-815-review-demo-seed-model-fit／issue-888-finalization-source-badge／issue-903-majority-fallback-removed／issue-907-exception-pool-screen-shell／issue-922-exception-breadcrumb／issue-596-assignment-readonly／issue-688-exception-pool-entry／issue-811-bypass-wording-task-surfaces）逐一核對皆透過資料層 `resolveExceptionPoolItem()` 直寫或僅引用動作字串於既有 fixture 斷言、不點擊 UI，判定不受影響，分兩批執行（23 passed + 65 passed）全數綠燈，0 迴歸。
  - **獨立審查裁決與修正**：`senior-code-reviewer` 發現一項阻擋級缺陷——spec delta 之 MODIFIED FR-095 遺漏正典現行的 v6.20.0（issue #913 sticky owner）、v6.21.0（issue #914 `hasLegitimateFinalizedValue`）、v6.25.0（issue #922 麵包屑）三段既有釐清，若照原樣 archive 會靜默刪除已合併的正典內容。已於 commit `2d451ae2` 逐字補回三段並加一則交叉參照註記（v6.21.0 引用已廢止 AC-4.56 處）。另一項非阻擋但需修正：切換處置後理由欄殘留文字仍會使確認鈕重新停用，此行為合理但未寫入規格、程式碼註解誤引 AC-4.74。已於同一 commit：新增 AC-4.75 第 6 點明文化此行為、修正 design.md D1/D3、修正程式碼註解引用、修正測試裡一句與實況不符的「理由仍為空」註解（斷言本身未變）。修正後重跑 Gate 1（`1 passed／0 failed`）、Gate 2（`0 error／15 warning`）、`issue-596-exception-pool.spec.ts` 全量（`7 passed`），並重生 screen inventory（commit `236b4fd9`）。
- [x] 1.4 正典 015 MAJOR bump（v6.26.0 起 → v7.0.0，BREAKING）：FR-095 新增「先選取、後確認的處置互動模型」段落並修正末句「AC-4.56、AC-4.57 維持原狀」之矛盾措辭；廢止 AC-4.56、AC-4.57（前提消失，ID 保留不重用）；新增 AC-4.72 ~ AC-4.75；「成功標準」清單新增 **SC-012**；補 Changelog。衍生檢視 `openspec/specs/annotation/015-annotation-workspace/spec.md` 之 FR-095 requirement 段落同步修正「一鍵完成」與正典矛盾之措辭（由 archive 之 dual-write 自動合併，propose 階段之 delta 已含正確內容）。[@main]
  - commit `e7d5e679`：正典 v6.26.0 → v7.0.0；FR-095 補齊 v6.20.0／v6.21.0／v6.25.0 三段既有釐清（獨立審查阻擋項修正後的完整版本）＋新增 v7.0.0 段落；AC-4.56／AC-4.57 廢止；AC-4.72~AC-4.75 新增；SC-012 新增；Changelog 新增 7.0.0 列；`specs/STATUS.md` annotation-015 列同步；`screen-inventory.md` 重生（SC 計數 45→46）。重跑全部閘門：Gate 1 `1 passed`、Gate 2 `0 error／15 warning`、`check-spec-artifacts.sh` passed、`check-user-path-map-freshness.mjs` → `PATH_MAP_FRESH`、`inventory-tests.sh` 11/11 PASS。
- [x] 1.5 Source-Verify：逐條 grep 衍生檢視引用（FR/AC ID、檔案路徑、issue 編號、程式碼識別字、被改寫條文子句）並記錄命中結果；`openspec archive exception-pool-select-then-confirm --yes`；`specs/STATUS.md` 之 `annotation-015` 列同步版本與狀態。[@main]
  - Source-Verify 逐條 grep（全部命中，詳見 PR body Source-Verify 區塊）：FR-095／FR-061／FR-093／FR-051／FR-063／FR-086／FR-097／FR-077、AC-4.56／AC-4.57／AC-4.69～AC-4.71（既存）／AC-4.72～AC-4.75／SC-011／SC-012（新增前 0 命中，符合預期）、v6.20.0／v6.21.0／v6.25.0 三段釐清文字、`EXCEPTION_POOL_ACTIONS`／`resolveExceptionPoolItem`／`hasLegitimateFinalizedValue`／`listReviewPoolItems`／`buildExceptionPoolItemRow`／`expandExceptionPoolAction`／`renderEntryBreadcrumb`／`crumbSamplePosTpl`／`datasetRecords`、issue #596／#907／#913／#914／#920／#921／#922、design.md D1／D3／D4、PR #919 合併記錄（`160d0bda`）。
  - `openspec archive 2026-09-26-exception-pool-select-then-confirm --yes` → 歸檔為 `openspec/changes/archive/2026-09-26-exception-pool-select-then-confirm/`，衍生檢視 `+1 added ／~1 modified`；CLI 把新增的 4 個 AC 情境置於檔案末尾另立一個同名 `### Requirement: FR-095`（因 propose 階段的 `## ADDED Requirements` 技巧性寫法所致，見 Gate 2 SOURCE_VERIFY_ID 討論），手動搬移合併進主要 FR-095 區塊（AC-4.69 之後）並刪除重複標題與其說明段落；同步修正衍生檢視頂部 Purpose 行版本號與「目前收錄」列舉補上本 change。CLI 已自動修正「一鍵完成」與正典矛盾之措辭（逐字核對通過）。
  - 手動整理後重跑 Gate 1（`No items found to validate` — 預期，change 已歸檔無在途項目）、Gate 2（`0 error／11 warning`，非本 change warning 數下降係因歸檔後 `TASK_FILE_COUNT_REVIEW`／`TASK_RED_EVIDENCE_REVIEW` 兩則僅對在途 change 生效）、`scripts/check-spec-artifacts.sh` → passed、`node scripts/check-user-path-map-freshness.mjs` → `PATH_MAP_FRESH`。
  - `specs/STATUS.md` `annotation-015` 列已於 1.4（commit `e7d5e679`）同步分支與版本摘要，狀態維持 `in-progress`（依專案慣例，`archived` 狀態需待本 PR 實際合併後由主 session 更新，本步驟不越權變更）。

## 非本 change 範圍

`arbitrationFinalizedSnapshot()` 之 `readReviewerSubmissions(...)[0]`（issue #975）與 `annotation-workspace.config.js` 另四處同構寬鬆判準（v6.21.0 Changelog 已記錄）不在本 change 處理範圍，另案追蹤。FR-095「最終例外處置畫面的外殼」（AC-4.69、SC-011）不受本 change 影響。
