# Make the `engines` field in package.json enforced rather than advisory.
# Without this, pnpm only prints a WARN and still exits 0 on a mismatched
# Node or pnpm version, so `engines` documents an intent nothing checks.
engine-strict=true
